---
phase: 04-management-ui-and-authentication
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/views/products.py
  - app/models/supplier_product.py
  - app/models/catalog.py
  - app/templates/products/supplier.html
  - app/templates/products/unmatched_catalog.html
  - app/templates/products/unmatched_supplier.html
  - app/static/js/products.js
autonomous: true
requirements:
  - DASH-05
  - MATCH-04
  - MATCH-05

must_haves:
  truths:
    - "Admin can see all supplier products with current prices and availability"
    - "Admin sees prom.ua catalog products that have no confirmed match"
    - "Admin sees supplier products (known brands, available) not matched to any catalog product"
    - "All product lists support pagination, search, and sorting"
    - "Admin can delete a product from the list"
    - "Admin can force-set a price on a product"
    - "Admin can mark a product as out-of-stock"
    - "Admin can manually set any product status"
  artifacts:
    - path: "app/views/products.py"
      provides: "Product management blueprint"
      exports: ["products_bp"]
    - path: "app/templates/products/supplier.html"
      provides: "Supplier products list page"
    - path: "app/templates/products/unmatched_catalog.html"
      provides: "Unmatched prom products page"
    - path: "app/templates/products/unmatched_supplier.html"
      provides: "Unmatched supplier products page"
  key_links:
    - from: "app/views/products.py"
      to: "app/models/supplier_product.py"
      via: "SupplierProduct queries with pagination"
      pattern: "SupplierProduct"
    - from: "app/views/products.py"
      to: "app/models/catalog.py"
      via: "PromProduct LEFT JOIN ProductMatch for unmatched"
      pattern: "PromProduct"
---

<objective>
Build product list pages: supplier products with prices/availability, unmatched prom.ua products, and unmatched supplier products.

Purpose: DASH-05 requires supplier product visibility, MATCH-04 requires seeing unmatched catalog products, MATCH-05 requires seeing unmatched supplier products. These lists help operators decide which products need attention.
Output: Three paginated product list pages with search and sorting.
</objective>

<execution_context>
@C:/Users/Yana/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Yana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-management-ui-and-authentication/04-RESEARCH.md
@.planning/phases/04-management-ui-and-authentication/04-01-SUMMARY.md

<interfaces>
From app/models/supplier_product.py:
```python
class SupplierProduct(db.Model):
    id, supplier_id, external_id, name, brand, model, article,
    price_cents (Integer), currency, available (Boolean), needs_review (Boolean),
    last_seen_at (DateTime)
    supplier = relationship("Supplier", backref="products")
```

From app/models/catalog.py:
```python
class PromProduct(db.Model):
    id, external_id, name, brand, model, article,
    price (Integer cents), currency, page_url, imported_at
```

From app/models/product_match.py:
```python
class ProductMatch(db.Model):
    supplier_product_id, prom_product_id, score, status
    (candidate|confirmed|rejected|manual)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create products blueprint with supplier products list, out-of-stock badge, and filtering</name>
  <files>
    app/views/products.py
    app/templates/products/supplier.html
  </files>
  <action>
    1. Create `app/views/products.py` with `products_bp = Blueprint("products", __name__)`:

       **GET /products/supplier** — Supplier products list (DASH-05):
       - Query all SupplierProduct with eager load of supplier relationship
       - Filter params: `supplier_id` (dropdown), `available` (yes|no|all, default=all), `needs_review` (yes|no|all), `search` (name substring), `sort` (name|price|brand|last_seen_at, default=name), `order` (asc|desc, default=asc), `page`, `per_page` (25|50|100)
       - Paginate with `db.paginate()`
       - Pass list of all suppliers for dropdown filter

       All routes decorated with `@login_required`.

    2. Register in `app/__init__.py`:
       ```python
       from app.views.products import products_bp
       app.register_blueprint(products_bp, url_prefix="/products")
       ```

    3. Create `app/templates/products/supplier.html` extending base.html:
       - Page title: "Товары поставщика"
       - Filter bar: supplier dropdown (all suppliers), availability filter, needs_review filter, search input, per-page selector
       - Table columns: Поставщик, Название, Бренд, Модель, Артикул, Цена (formatted from price_cents with currency), Наличие (badge: green "В наличии" / red "Нет в наличии"), Изменён (badge, see below), Ревью (badge if needs_review), Обновлено (last_seen_at formatted)
       - **"Changed" indicator** (per user decision): Add a `last_modified_at` DateTime field to SupplierProduct model (nullable, set by sync pipeline when any field changes). In the template, if `last_modified_at` is newer than the previous sync's `started_at`, show a yellow badge "Изменён". NOTE: Full diff (old/new values) requires an audit table (deferred — backend detection logic). For now, the badge indicates that the product was updated in the latest sync. The `last_modified_at` field should be set in `sync_pipeline.py` during the save stage when `products_updated > 0`.
       - "Нет в наличии" badge: `<span class="badge bg-danger">Нет в наличии</span>` per user decision
       - Sortable column headers
       - Pagination controls
       - Show total count: "Всего: N товаров"

    4. Update base.html nav: replace placeholder Products > Supplier Products link with `{{ url_for('products.supplier_list') }}`
  </action>
  <verify>
    <automated>cd C:/Projects/labresta-sync && uv run python -c "
from app import create_app
from app.extensions import db
app = create_app()
with app.app_context():
    from app.models.user import User
    u = User.query.filter_by(email='test@verify.com').first()
    if not u:
        u = User(email='test@verify.com', name='Verifier', role='admin')
        u.set_password('testpass123')
        db.session.add(u)
        db.session.commit()
with app.test_client() as c:
    r = c.get('/products/supplier')
    assert r.status_code == 302, f'Supplier products should require auth: {r.status_code}'
    print('Supplier products page requires auth')
    # Login and verify functional output
    c.post('/auth/login', data={'email': 'test@verify.com', 'password': 'testpass123', 'csrf_token': 'test'}, follow_redirects=True)
    r = c.get('/products/supplier')
    assert r.status_code == 200, f'Should render when logged in: {r.status_code}'
    print('Supplier products page renders when authenticated')
    print('All checks passed')
"</automated>
  </verify>
  <done>Supplier products page renders with all columns, filters, sorting, pagination, and out-of-stock badges</done>
</task>

<task type="auto">
  <name>Task 2: Create unmatched product lists — prom.ua without match and supplier without match</name>
  <files>
    app/views/products.py
    app/templates/products/unmatched_catalog.html
    app/templates/products/unmatched_supplier.html
  </files>
  <action>
    1. Add to `app/views/products.py`:

       **GET /products/unmatched-catalog** — Unmatched prom.ua products (MATCH-04):
       - Query PromProduct that have NO confirmed/manual ProductMatch:
         ```python
         # Subquery: prom_product_ids that have a confirmed or manual match
         matched_ids = db.session.execute(
             select(ProductMatch.prom_product_id).where(
                 ProductMatch.status.in_(["confirmed", "manual"])
             )
         ).scalars().all()
         # Main query: PromProduct NOT IN matched_ids
         query = select(PromProduct).where(PromProduct.id.not_in(matched_ids))
         ```
       - Filter params: `search` (name substring), `brand` (dropdown of distinct brands), `sort`, `order`, `page`, `per_page`
       - Paginate results

       **GET /products/unmatched-supplier** — Supplier products not in store (MATCH-05):
       - Query SupplierProduct that are available, have a brand, but have NO confirmed/manual ProductMatch:
         ```python
         matched_supplier_ids = db.session.execute(
             select(ProductMatch.supplier_product_id).where(
                 ProductMatch.status.in_(["confirmed", "manual"])
             )
         ).scalars().all()
         query = select(SupplierProduct).where(
             SupplierProduct.id.not_in(matched_supplier_ids),
             SupplierProduct.available == True,
             SupplierProduct.brand.isnot(None),
         )
         ```
       - Filter params: `supplier_id`, `brand`, `search`, `sort`, `order`, `page`, `per_page`

    2. Create `app/templates/products/unmatched_catalog.html` extending base.html:
       - Page title: "Товары prom.ua без матча"
       - Explanation text: "Товары из каталога prom.ua, для которых не найден подтверждённый матч с товаром поставщика."
       - Filter bar: brand dropdown, search input
       - Table columns: Название, Бренд, Модель, Артикул, Цена, Ссылка на prom.ua (page_url as link, if available)
       - Sortable headers, pagination
       - Show total count

    3. Create `app/templates/products/unmatched_supplier.html` extending base.html:
       - Page title: "Товары поставщика без матча"
       - Explanation text: "Товары от поставщика с известным брендом, которых нет в вашем магазине. Рассмотрите возможность добавить их."
       - Filter bar: supplier dropdown, brand dropdown, search input
       - Table columns: Поставщик, Название, Бренд, Модель, Артикул, Цена поставщика, Наличие
       - Sortable headers, pagination
       - Show total count

    4. Update base.html nav: replace placeholder Products links:
       - "Товары поставщика" → `{{ url_for('products.supplier_list') }}`
       - "Каталог без матча" → `{{ url_for('products.unmatched_catalog') }}`
       - "Поставщик без матча" → `{{ url_for('products.unmatched_supplier') }}`
       Use a dropdown menu under "Товары" nav item.
  </action>
  <verify>
    <automated>cd C:/Projects/labresta-sync && uv run python -c "
from app import create_app
from app.extensions import db
app = create_app()
with app.app_context():
    from app.models.user import User
    u = User.query.filter_by(email='test@verify.com').first()
    if not u:
        u = User(email='test@verify.com', name='Verifier', role='admin')
        u.set_password('testpass123')
        db.session.add(u)
        db.session.commit()
with app.test_client() as c:
    for path in ['/products/unmatched-catalog', '/products/unmatched-supplier']:
        r = c.get(path)
        assert r.status_code == 302, f'{path} should require auth: {r.status_code}'
    print('Unmatched product pages require auth')
    # Login and verify functional output
    c.post('/auth/login', data={'email': 'test@verify.com', 'password': 'testpass123', 'csrf_token': 'test'}, follow_redirects=True)
    for path in ['/products/unmatched-catalog', '/products/unmatched-supplier']:
        r = c.get(path)
        assert r.status_code == 200, f'{path} should render when logged in: {r.status_code}'
    print('Unmatched product pages render when authenticated')
    print('All checks passed')
"</automated>
  </verify>
  <done>Unmatched prom.ua products and unmatched supplier products pages render with correct queries, filters, sorting, and pagination</done>
</task>

<task type="auto">
  <name>Task 3: Add product management write endpoints — delete, force-price, mark out-of-stock, set status</name>
  <files>
    app/views/products.py
    app/templates/products/supplier.html
    app/static/js/products.js
  </files>
  <action>
    1. Add write endpoints to `app/views/products.py` (per locked user decision: "Full manual product management through UI: delete product, force price, mark out-of-stock, set any status"):

       **POST /products/supplier/&lt;int:product_id&gt;/mark-unavailable** — Mark product out of stock:
       - Set `SupplierProduct.available = False` and `SupplierProduct.needs_review = False`
       - Return JSON `{"status": "ok", "message": "Товар отмечен как отсутствующий"}`

       **POST /products/supplier/&lt;int:product_id&gt;/mark-available** — Mark product back in stock:
       - Set `SupplierProduct.available = True`
       - Return JSON `{"status": "ok", "message": "Товар отмечен как в наличии"}`

       **POST /products/supplier/&lt;int:product_id&gt;/force-price** — Force-set product price:
       - JSON body: `{"price_cents": 15000, "currency": "UAH"}`
       - Set `SupplierProduct.price_cents` and `SupplierProduct.currency` to provided values
       - Add a flag `SupplierProduct.price_forced = True` (Boolean, default False) — so sync pipeline won't overwrite manual price
       - Return JSON `{"status": "ok", "message": "Цена принудительно установлена"}`

       **POST /products/supplier/&lt;int:product_id&gt;/delete** — Soft-delete product:
       - Add `SupplierProduct.is_deleted = Boolean(default=False)` field if not present
       - Set `is_deleted = True` and `available = False`
       - Return JSON `{"status": "ok", "message": "Товар удалён"}`
       - All product list queries must filter `is_deleted == False`

       **POST /products/supplier/&lt;int:product_id&gt;/set-status** — Set arbitrary status fields:
       - JSON body: `{"needs_review": true/false, "available": true/false}`
       - Update the specified fields
       - Return JSON `{"status": "ok"}`

       All write endpoints decorated with `@login_required`. Return 404 if product not found.

    2. Add `price_forced` and `is_deleted` columns to `SupplierProduct` model in `app/models/supplier_product.py`:
       ```python
       price_forced = Column(Boolean, default=False, server_default='0')
       is_deleted = Column(Boolean, default=False, server_default='0')
       ```
       Add Alembic migration or use `db.create_all()` fallback.

    3. Update `app/templates/products/supplier.html` — add action buttons to each row:
       - "Нет в наличии" / "В наличии" toggle button (btn-outline-warning / btn-outline-success)
       - "Установить цену" button (btn-outline-primary) — opens a small inline form or modal with price input
       - "Удалить" button (btn-outline-danger) with confirmation dialog
       - Show "Принудительная цена" badge on rows where `price_forced == True`
       - Show "Удалён" badge on deleted items (only visible with special filter)

    4. Create `app/static/js/products.js`:
       - AJAX handlers for each action button using fetch() with CSRF token
       - On success: update the row in-place (change badge, disable button) without full page reload
       - Force-price modal: input field for price in UAH (convert to cents before sending)
       - Delete confirmation: `confirm("Удалить товар? Это действие можно отменить.")` before POST
       - Include in supplier.html template: `{% block scripts %}<script src="{{ url_for('static', filename='js/products.js') }}"></script>{% endblock %}`

    5. Update product list queries in Task 1 and Task 2 routes to exclude `is_deleted == True` by default. Add an optional `show_deleted=true` query param for admin use.
  </action>
  <verify>
    <automated>cd C:/Projects/labresta-sync && uv run python -c "
from app import create_app
from app.extensions import db
app = create_app()
with app.app_context():
    from app.models.supplier_product import SupplierProduct
    # Verify new columns exist
    assert hasattr(SupplierProduct, 'price_forced'), 'Missing price_forced column'
    assert hasattr(SupplierProduct, 'is_deleted'), 'Missing is_deleted column'
    print('SupplierProduct model has price_forced and is_deleted columns')
    from app.models.user import User
    u = User.query.filter_by(email='test@verify.com').first()
    if not u:
        u = User(email='test@verify.com', name='Verifier', role='admin')
        u.set_password('testpass123')
        db.session.add(u)
        db.session.commit()
with app.test_client() as c:
    # Login
    c.post('/auth/login', data={'email': 'test@verify.com', 'password': 'testpass123', 'csrf_token': 'test'}, follow_redirects=True)
    # Test that action endpoints exist (will 404 for missing product, but not 405 Method Not Allowed)
    r = c.post('/products/supplier/99999/mark-unavailable')
    assert r.status_code in (404, 200), f'mark-unavailable should accept POST: {r.status_code}'
    r = c.post('/products/supplier/99999/force-price', json={'price_cents': 10000, 'currency': 'UAH'})
    assert r.status_code in (404, 200), f'force-price should accept POST: {r.status_code}'
    r = c.post('/products/supplier/99999/delete')
    assert r.status_code in (404, 200), f'delete should accept POST: {r.status_code}'
    print('All product management write endpoints respond to POST')
    print('All checks passed')
"</automated>
  </verify>
  <done>Product management write endpoints work: mark out-of-stock, force price, delete, set status. Action buttons in supplier products table with AJAX handlers.</done>
</task>

</tasks>

<verification>
1. /products/supplier shows all supplier products with prices, availability badges
2. Filter by supplier, by availability — correct products shown
3. /products/unmatched-catalog shows prom.ua products without confirmed match
4. /products/unmatched-supplier shows available supplier products with brand but no match
5. All lists paginate correctly (25/50/100)
6. Search and sorting work on all pages
7. Navigation dropdown links to all three product pages
</verification>

<success_criteria>
- Supplier products list with all columns, filters, out-of-stock badge
- Unmatched prom.ua products list with brand filter
- Unmatched supplier products list with supplier/brand filters
- All lists support pagination, search, and column sorting
- Navigation properly links to all product pages
</success_criteria>

<output>
After completion, create `.planning/phases/04-management-ui-and-authentication/04-04-SUMMARY.md`
</output>
