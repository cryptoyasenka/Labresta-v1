# chunk-014 — ручная проверка для Yana (русский)

**Source:** `horoshop-export 13.05.26.xlsx` chunk-014 (70 SKU)
**Apply key:** `Артикул` (scoped per row)
**Status:** ГОТОВ 70/70

Здесь собираю всё, что требует твоего подтверждения (не авто-фиксы). Авто-фиксы по locked-паттернам перечислены в сводках по батчам, отдельного подтверждения не требуют. Открытые вопросы накапливаются в нумерованный список и финализируются при закрытии chunk-014.

---

## Батч SKU 1-8 (8/70)

SKU 1-8 — смешанный открывающий блок chunk-014: копчение/низкотемпературные печи + старт блока рисоварок. **SKU 1** Alto-Shaam 1750-SK/DX SD — прямое продолжение Alto-Shaam-хвоста chunk-013 (sibling SKU 61, DX-вариант), без правок (RU самостоятелен) + 1 AUTO-орфо. **SKU 2** Hendi 199961 коптильный пистолет — без правок (RU самостоятелен). **SKU 3** Hendi 238486 электрическая коптильня — 🔴 (эталона нет → перевод по источнику, multi-line `<h2>`+`<ul>`) + Назв.мод (RU) UA-leak. **SKU 4** Hendi 199664 стеклянный купол — 🔴 (зеркало канон-формы Hendi-купол chunk-013 SKU 58/60) + Назв.мод (RU) UA-leak. **SKU 5** Helia Smoker HELIA 24 — 🔴 (по источнику, single-line) + реальная дробь `1.5`→`1,5`. **SKU 6** Moduline CHS082E — без правок (RU самостоятелен) + **новый Открытый вопрос #1** (model-код рассинхрон). **SKU 7** Moduline CHS052E — sibling SKU 6, без правок. **SKU 8** CUCKOO CR-3521 рисоварка — без правок (RU самостоятелен), старт блока рисоварок. **🔴 в 3 из 8** (SKU 3/4/5). Без правок: SKU 1/2/6/7/8. Назв.мод (RU) UA-leak триплеты: SKU 3/4. Реальная дробь: SKU 5. Новый Открытый вопрос #1 (SKU 6).

### Стандартно применено (locked-паттерны, подтверждения НЕ требуют)
- **🔴 RU=UA полная укр. копия → AUTO полный перевод тег-в-tag:** SKU 3 (Hendi 238486 коптильня — эталона нет, по источнику, multi-line), SKU 4 (Hendi 199664 купол — зеркало канон-формы chunk-013 SKU 58/60), SKU 5 (Helia Smoker — по источнику, single-line). House corpus без ё (`решетка`/`оснащен`); `гачка→крючка`, `м'яса→мяса`, `коптильні тирси→коптильные опилки`; латинская x в габаритах → глоб.B (в тексте дословно).
- **Назв.мод (RU) UA-leak → триплет (перевод по корректному RU Название):** SKU 3 `ЕЛЕКТРИЧНА КОПТИЛЬНЯ`→`ЭЛЕКТРИЧЕСКАЯ КОПТИЛЬНЯ`, SKU 4 `Скляний купол`→`Стеклянный купол`.
- **Реальная `.`-дробь `N.N`→`N,N` обе локали:** SKU 5 `1.5`→`1,5` (поле «Споживана потужність», в т.ч. внутри 🔴-перевода RU + точечно в UA; прецедент chunk-013 SKU 60 `0.53`).
- **AUTO-орфо (очевидный typo, откат-нота):** SKU 1 RU `сенсорная панель упралнения`→`управления` (гарбл `вл`→`лн`, единственная цель — стандартная фраза «панель управления»).
- **Чистые SKU (RU — корректный самостоятельный перевод, без правок описания):** SKU 1 Alto-Shaam 1750-SK/DX SD (1777550136), 2 Hendi 199961 (908945355), 6 Moduline CHS082E (2098339490), 7 Moduline CHS052E (2098369196), 8 CUCKOO CR-3521 (1160804329). 🔴 — ни в одном из этих.

### Заслуживает внимания (информативно, не блокирует)
- **SKU 2 — RU статус-метка «снято с производства».** RU Название/Назв.мод = `Коптильный пистолет Hendi 199961 снято с производства`; UA = `Коптильний пістолет Hendi 199961` (без метки). Не число/код-рассинхрон — мерч-статус, правдоподобно намеренный (магазин помечает снятые позиции только в RU). Оставлено faithful, **не авто-удалено**, в Открытые вопросы НЕ вынесено. Если хочешь единообразия (метку в UA тоже / убрать совсем) — скажи.
- **SKU 6/7 — RU `Характаристики:` typo.** В Moduline-шаблоне (оба SKU) RU «Характаристики:» вместо «Характеристики:» (гласная `а`→`е`). Source-артефакт, рекуррент; оставлено faithful, не авто (вне категории «удвоение/выпадение слога»). Подтверди, если хочешь авто-нормализацию `Характаристики`→`Характеристики` по всему чанку при встрече.
- **SKU 1 — UA склейка `ПічAlto-Shaam`.** UA Название/Назв.мод без пробела между `Піч` и `Alto-Shaam` (source-артефакт). RU-форма `Печь Alto-Shaam` корректна (пробел есть). UA оставлено faithful.
- **SKU 2 — `мм мм` дубль.** `Габарити/Габариты 68х158 мм мм` — удвоение единицы идентично в ОБЕИХ локалях (симметрично, не рассинхрон). Faithful, не авто.

### Сводка батча

| SKU | Артикул | Товар | Тип | Действие |
|----|---------|-------|-----|----------|
| 1 | 1777550136 | Alto-Shaam 1750-SK/DX SD печь | без правок + AUTO-орфо | RU самостоятелен; `упралнения`→`управления` |
| 2 | 908945355 | Hendi 199961 коптильный пистолет | без правок | RU самостоятелен; статус-метка soft note |
| 3 | 964765301 | Hendi 238486 электр. коптильня | 🔴 + Назв.мод | полный RU перевод + триплет |
| 4 | 2123162134 | Hendi 199664 стеклянный купол | 🔴 + Назв.мод | полный RU перевод + триплет |
| 5 | 490002576 | Helia Smoker HELIA 24 | 🔴 + дробь | полный RU перевод + `1.5`→`1,5` обе локали |
| 6 | 2098339490 | Moduline CHS082E печь | без правок + Q#1 | RU самостоятелен; model-код рассинхрон → Открытый вопрос #1 |
| 7 | 2098369196 | Moduline CHS052E печь | без правок | RU самостоятелен (sibling 6, код консистентен) |
| 8 | 1160804329 | CUCKOO CR-3521 рисоварка 6,3 л | без правок | RU самостоятелен; старт блока рисоварок |

---

## Батч SKU 9-16 (16/70)

SKU 9-16 — продолжение блока рисоварок (SKU 8-15) + хвост: пароварка SKU 16. **SKU 9** Bartscher A150513 8 л — 🔴 (RU-поле было полной укр. копией → авто-перевод тег-в-tag, single-line). **SKU 10** GGM RKF100 5,4 л — без правок (RU самостоятелен). **SKU 11** GGM Gastro RKK100 8,2 л — sibling 10, без правок. **SKU 12** CUCKOO SR-4600 4,6 л — без правок (RU самостоятелен, multi-line). **SKU 13** Bartscher 150528 1,8 л — 🔴 (по источнику, multi-line). **SKU 14** HENDI 240410 1,8 л паровая — 🔴 + Назв.мод (RU) UA-leak. **SKU 15** HENDI 240403 5,4 л — без правок (RU самостоятелен, реструктурирован). **SKU 16** HURAKAN HKN-DS1 пароварка Dim Sum — 🔴 + Назв.мод (RU) UA-leak. **🔴 в 4 из 8** (SKU 9/13/14/16). Без правок: SKU 10/11/12/15. Назв.мод (RU) UA-leak триплеты: SKU 14/16. Реальных дробей нет. Новых Открытых вопросов нет.

### Стандартно применено (locked-паттерны, подтверждения НЕ требуют)
- **🔴 RU=UA полная укр. копия → AUTO полный перевод тег-в-tag:** SKU 9 (Bartscher A150513 — single-line, эталона нет, по источнику), SKU 13 (Bartscher 150528 — multi-line, по источнику), SKU 14 (HENDI 240410 — multi-line `<h2>`/`<p>`/`<ul>`), SKU 16 (HURAKAN HKN-DS1 — multi-line `<h2>`+`<ul>`+хвостовые `<p>`). House corpus без ё (`объем`/`котел`/`подойдет`); `об'єм/м'яса/під'єднання/пом'якшувач`→`объем/мяса/подключение/смягчитель` (энтити `&#39;` снят); энтити `&mdash;` сохранён; латинская x в габаритах → глоб.B (в тексте дословно); пустой `<p> </p>` SKU 16 сохранён дословно.
- **Назв.мод (RU) UA-leak → триплет (перевод собственного содержимого RU-поля):** SKU 14 `Рисоварка 1,8 л HENDI 240410 з функцією приготування на пару`→`…с функцией приготовления на пару` (совпадает с уже корректным RU Название), SKU 16 `Пароварка Hurakan HKN-DS1 для дим-самів`→`…для дим-самов` (укр. род. мн. `-ів`→рус. `-ов`; прецедент chunk-013 SKU 50 — переводим содержимое RU-поля).
- **Voltage faithful (значение НЕ трогаю — НИКОГДА):** только SKU 16 `380 В` (UA-метка `Напруга`→RU `Напряжение`, значение `380 В` дословно).
- **Реальная `.`-дробь:** в батче НЕТ (все числа уже с запятой или целые — no-op).
- **Чистые SKU (RU — корректный самостоятельный перевод, без правок описания):** SKU 10 GGM RKF100 (490711823), 11 GGM Gastro RKK100 (490716913), 12 CUCKOO SR-4600 (518661589), 15 HENDI 240403 (518661593). 🔴 — ни в одном из этих.

### Заслуживает внимания (информативно, не блокирует)
- **SKU 15 — RU комплектация богаче UA (антипригарный коврик).** RU «В комплекте: лопатка, мерная чаша **и антипригарный коврик**»; UA «В комплекті: лопатка, мірна чаша» (коврик НЕ упомянут). Это не число/код-рассинхрон, а более полное RU-описание комплекта (по locked-правилу — soft note, аналог chunk-012 SKU 1 `8+8+8 л` / chunk-010 SKU 47; richer-RU faithful, **не авто, в Открытые вопросы НЕ вынесено**). Контекст для оценки: в SKU 12 этого же батча магазин явно фиксирует наличие/отсутствие коврика как спец-пункт (UA/RU «Силиконового коврика нет»), поэтому различие комплектации в SKU 15 более заметное. Если хочешь выровнять (добавить коврик в UA / убрать из RU) — скажи; правка к Артикул=518661593.
- **SKU 9 — UA-русизм `порций`.** UA-источник `<li>На 25-40 порций.</li>` использует рус. форму `порций` (UA-норма — `порцій`). UA оставлено faithful; RU-перевод `На 25-40 порций.` корректен. Не рассинхрон.
- **SKU 16 — META sibling-leak.** RU/UA META keywords содержат `cuckoo, sr 4600, куко, корейская/корейська…` (хвост от рисоварок CUCKOO) в товаре HURAKAN-пароварке. META **always faithful** — не правится. Симметрично UA↔RU.

### Сводка батча

| SKU | Артикул | Товар | Тип | Действие |
|----|---------|-------|-----|----------|
| 9 | 475144743 | Bartscher A150513 рисоварка 8 л | 🔴 | полный RU перевод тег-в-tag (single-line) |
| 10 | 490711823 | GGM RKF100 рисоварка 5,4 л | без правок | RU самостоятелен |
| 11 | 490716913 | GGM Gastro RKK100 рисоварка 8,2 л | без правок | RU самостоятелен (sibling 10) |
| 12 | 518661589 | CUCKOO SR-4600 рисоварка 4,6 л | без правок | RU самостоятелен; глоб.B латин. x |
| 13 | 518661591 | Bartscher 150528 рисоварка 1,8 л | 🔴 | полный RU перевод тег-в-tag (multi-line) |
| 14 | 518661592 | HENDI 240410 рисоварка 1,8 л паровая | 🔴 + Назв.мод | полный RU перевод + триплет |
| 15 | 518661593 | HENDI 240403 рисоварка 5,4 л | без правок | RU самостоятелен; soft note комплектация |
| 16 | 732745638 | HURAKAN HKN-DS1 пароварка Dim Sum | 🔴 + Назв.мод | полный RU перевод + триплет (`дим-самів`→`дим-самов`) |

---

## Батч SKU 17-24 (24/70)

SKU 17-24 — хвост блока рисоварок + термосы для риса. **SKU 17** Bartscher A150512 подогреватель риса — 🔴 (RU-поле было полной укр. копией → авто-перевод тег-в-tag, multi-line) + Назв.мод (RU) UA-leak. **SKU 18** AIRHOT RC-5 рисоварка — 🔴 (multi-line `<h2>`+`<ul>`, voltage `220`). **SKU 19** FROSTY RC-30 рисоварка — 🔴 (multi-line с пустым `<p>&nbsp;</p>` и TAB-отступами). **SKU 20** FROSTY RC-40A рисоварка — 🔴 (single-line `<p>…</p> <ul>`, литерал `—`). **SKU 21** TIGER JFM-390P термос для риса 3,9 кг — без правок (RU самостоятелен корректен). **SKU 22** TIGER JFM-570P термос для риса 5,7 кг — sibling 21, без правок. **SKU 23** REEDNEE SA8236 рисоварка 8 л — 🔴 (single-line `<h2>`+`<p>`+`<ul>`). **SKU 24** REEDNEE SB8155 рисоварка 10 л — sibling 23, 🔴. **🔴 в 6 из 8** (SKU 17/18/19/20/23/24). Без правок: SKU 21/22. Назв.мод (RU) UA-leak триплет: только SKU 17. Реальных дробей нет. Новых Открытых вопросов нет.

### Стандартно применено (locked-паттерны, подтверждения НЕ требуют)
- **🔴 RU=UA полная укр. копия → AUTO полный перевод тег-в-tag:** SKU 17 (Bartscher A150512 — multi-line `<p>…</p> <ul>`), SKU 18 (AIRHOT RC-5 — multi-line `<h2>`+`<ul>`, хвостовые пробелы `. </h2>` / ` -13` / `220 ` сохранены дословно), SKU 19 (FROSTY RC-30 — multi-line с пустым абзацем `<p>&nbsp;</p>` и TAB-отступом `\t<li>` ×5 + хвостовые 2 newline), SKU 20 (FROSTY RC-40A — single-line `<p>…</p> <ul>`, литерал `—`), SKU 23 (REEDNEE SA8236 — single-line `<h2>`+`<p>`+`<ul>`), SKU 24 (REEDNEE SB8155 — sibling 23). House corpus без ё (`объем`/`емкость`); `об'єм/Об'єм`→`объем/Объем` (энтити `&#39;` снят); `склянка`→`стакан` (SKU 23/24); энтити `&mdash;`/`&nbsp;` сохранены (SKU 19); литерал `—` сохранён (SKU 20/23/24); латинская x в габаритах → глоб.B (в тексте дословно); кириллическая х — no-op (SKU 17/18).
- **Назв.мод (RU) UA-leak → триплет (перевод собственного содержимого RU-поля):** только SKU 17 `Підігрівач рису Bartscher A150512`→`Подогреватель риса Bartscher A150512` (совпадает с уже корректным RU Название; прецедент chunk-013 SKU 50 / chunk-014 SKU 14/16). SKU 18-24 — Назв./Назв.мод UA==RU валидно (НЕ leak, триплет НЕ нужен).
- **Voltage faithful (значение НЕ трогаю — НИКОГДА):** только SKU 18 `220` (UA-метка `Напруга`→RU `Напряжение`, значение `220` дословно с хвостовым пробелом).
- **Реальная `.`-дробь:** в батче НЕТ (все числа уже с запятой / целые / `7,0` — no-op).
- **Чистые SKU (RU — корректный самостоятельный перевод, без правок описания):** SKU 21 TIGER JFM-390P (1160811432), 22 TIGER JFM-570P (1160818677). 🔴 — ни в одном из этих.

### Заслуживает внимания (информативно, не блокирует)
- **SKU 21/22 — META sibling-leak `CUCKOO/CR-…`.** RU/UA META keywords карточек термосов TIGER содержат хвост от рисоварок CUCKOO (`CUCKOO, SR-4600, куко, корейск…, CR-4600/3511/3521`). META **always faithful** — не правится. Симметрично UA↔RU.
- **SKU 18 — UA `Об'єм, л -13` странный формат.** UA-источник в строке объёма: `Об'єм, л -13` (пробел + дефис вместо тире-разделителя как в других строках `— NN`). Source-артефакт; RU-перевод `Объем, л -13` сохраняет формат faithful. Не число/код-рассинхрон (значение `13` не меняется). Если хочешь нормализовать пунктуацию строки — скажи; правка к Артикул=1033556595.
- **SKU 23/24 — UA-русизм `порций`.** UA-источник `На 30-40 порций` / `На 40-50 порций` использует рус. форму `порций` (UA-норма — `порцій`; рекуррент, ср. SKU 9 этого chunk). UA оставлено faithful; RU-перевод корректен. Не рассинхрон.

### Сводка батча

| SKU | Артикул | Товар | Тип | Действие |
|----|---------|-------|-----|----------|
| 17 | 885027031 | Bartscher A150512 подогреватель риса | 🔴 + Назв.мод | полный RU перевод + триплет (`Підігрівач рису`→`Подогреватель риса`) |
| 18 | 1033556595 | AIRHOT RC-5 рисоварка | 🔴 | полный RU перевод тег-в-tag (multi-line); voltage `220` faithful |
| 19 | 1110574295 | FROSTY RC-30 рисоварка | 🔴 | полный RU перевод (multi-line, `<p>&nbsp;</p>`+TAB сохранены) |
| 20 | 1110576512 | FROSTY RC-40A рисоварка | 🔴 | полный RU перевод (single-line, литерал `—`); глоб.B латин. x |
| 21 | 1160811432 | TIGER JFM-390P термос для риса 3,9 кг | без правок | RU самостоятелен; META sibling-leak soft note |
| 22 | 1160818677 | TIGER JFM-570P термос для риса 5,7 кг | без правок | RU самостоятелен (sibling 21); META soft note |
| 23 | 1527065711 | REEDNEE SA8236 рисоварка 8 л | 🔴 | полный RU перевод тег-в-tag (single-line); `склянка`→`стакан` |
| 24 | 1527074785 | REEDNEE SB8155 рисоварка 10 л | 🔴 | полный RU перевод тег-в-tag (sibling 23) |

---

## Батч SKU 25-32 (32/70)

SKU 25-32 — пароварка + блок рисоварок (Casta / Frosty / GoodFood ×2 / Saro ×2 / Bartscher / Hurakan). **SKU 25** Casta L7DSE800BA пароварка (8 корзин) — без правок (RU самостоятелен корректен). **SKU 26** Frosty RC-22 рисоварка — без правок. **SKU 27** GoodFood ERC42A рисоварка — без правок. **SKU 28** GoodFood ERC56A рисоварка — sibling 27, без правок. **SKU 29** SARO JULIUS рисоварка — без правок описания + RU `квт`→`кВт` AUTO+откат + реальная дробь `1.95` ОБЕ локали. **SKU 30** SARO RICO рисоварка — sibling 29, без правок описания + RU `квт`→`кВт` AUTO+откат + реальные дроби `1.6`/`5.1`/`4.2` ОБЕ локали. **SKU 31** Bartscher А150513 рисоварка — 🔴 (RU-поле было полной укр. копией → авто-перевод тег-в-tag, single-line + строка `</ul>` отдельно). **SKU 32** HURAKAN HKN-SR180 рисоварка — 🔴 (multi-line `<p>…</p> <ul>` + построчные `<li>` ×9). **🔴 в 2 из 8** (SKU 31/32). Без правок: SKU 25/26/27/28 чистые + 29/30 (описание чистое, только targeted fixes). Назв.мод (RU) UA-leak триплет: нет. Новых нумерованных Открытых вопросов нет.

### Стандартно применено (locked-паттерны, подтверждения НЕ требуют)
- **🔴 RU=UA полная укр. копия → AUTO полный перевод тег-в-tag:** SKU 31 (Bartscher А150513 — single-line `<p>…</p> <p><strong>…</strong></p> <ul> <li>…</li>` + строка `</ul>` отдельно, 2 строки), SKU 32 (HURAKAN HKN-SR180 — multi-line: строка1 `<p>…риса. </p> <p><strong>…</strong></p> <ul>` с хвостовым пробелом `риса. `, затем построчные `<li>` ×9 + `</ul>`). House corpus без ё; `об&#39;єм`→`объем` (энтити `&#39;` снят); `килимок`→`коврик` (31/32); `мірний стакан`→`мерный стакан` (32); `385*480*345` `*`-мультипликатор — no-op дословно (31, НЕ латин. x); `445x445x418` латинская x → глоб.B в тексте дословно (32); литерал `-` в `діаметр - 320` сохранён (31).
- **RU единица `квт`→`кВт` (рекуррент Saro, AUTO + откат-нота, единогласные siblings + кросс-чанк прецедент chunk-010/011/012/013):** SKU 29 (`Мощность, квт 1.95`→`Мощность, кВт 1,95`), SKU 30 (`Мощность, квт 1.6`→`Мощность, кВт 1,6`). UA `квт` — faithful (UA-сторона регистр НЕ авто-правится, locked).
- **Реальная `.`-дробь → `,` ОБЕ локали:** SKU 29 `1.95`→`1,95` (RU+UA); SKU 30 `1.6`→`1,6`, `5.1`→`5,1`, `4.2`→`4,2` (RU+UA). (SKU 28 `9,40`/`10,70` — `,NN` trailing-zero артефакт, no-op, НЕ реальная дробь; SKU 30 строка `…/ 1,6 кВт` уже `,` — no-op.)
- **Назв.мод (RU) UA-leak → триплет:** НЕТ (все Назв./Назв.мод UA==RU валидны — НЕ leak, триплет НЕ нужен).
- **Voltage faithful (значение НЕ трогаю — НИКОГДА):** SKU 25 `380`, SKU 27/28/29/30/32 `220`, SKU 31 `230`; в 🔴 31/32 метка `Підключення`/`Напруга`→`Подключение`/`Напряжение`, значение нетронуто.
- **Чистые SKU (RU — корректный самостоятельный перевод, без правок описания):** SKU 25 Casta пароварка (1836021103), 26 Frosty RC-22 (2044642124), 27 GoodFood ERC42A (2172881635), 28 GoodFood ERC56A (2172889513); SKU 29/30 Saro — описание чистое (только targeted fixes выше). 🔴 — ни в одном из этих.

### Заслуживает внимания (информативно, не блокирует)
- **SKU 25 — META sibling-leak `CUCKOO, SR-4600, куко, HURAKAN, HKN-DS1, Dim Sum`.** RU/UA META keywords карточки пароварки Casta содержат хвост от рисоварок CUCKOO/HURAKAN. META **always faithful** — не правится. Симметрично UA↔RU.
- **SKU 29/30 — RU `Подключение, в 220` строчная `в` (метка).** RU-метка строки напряжения со строчной `в` (faithful, прецедент chunk-012 SKU 7/8). Значение `220` не трогаю. Не рассинхрон.
- **SKU 29 — UA META `Рисувка` typo + RU `режиме термос`.** UA-источник META содержит `Рисувка` (typo от `Рисоварка`) — UA faithful, НЕ авто (UA typos locked). RU `режиме термос` (без `-а`) — мелкий UA-faithful, не эскалируется.
- **SKU 26/27/28 — UA `&ndash;` / RU литерал `-`.** Различие источников (UA энтити vs RU дефис). Faithful, не нормализую.
- **SKU 31 — дубль-листинг с SKU 9 (Bartscher A150513 / А150513).** Другой Артикул (2566572706 vs 475144743), другой текст описания, scoped независимо. НЕ customer-facing рассинхрон — не Открытый вопрос.

### Сводка батча

| SKU | Артикул | Товар | Тип | Действие |
|----|---------|-------|-----|----------|
| 25 | 1836021103 | Casta L7DSE800BA пароварка (8 корзин) | без правок | RU самостоятелен; META sibling-leak soft note; voltage `380` faithful |
| 26 | 2044642124 | Frosty RC-22 рисоварка | без правок | RU самостоятелен; UA `&ndash;`/RU `-` soft note |
| 27 | 2172881635 | GoodFood ERC42A рисоварка | без правок | RU самостоятелен; voltage `220` faithful; глоб.B латин. x |
| 28 | 2172889513 | GoodFood ERC56A рисоварка | без правок | RU самостоятелен (sibling 27); `9,40`/`10,70` no-op |
| 29 | 2331720070 | SARO JULIUS рисоварка | RU `квт`→`кВт` + дробь | `Мощность, квт 1.95`→`кВт 1,95` (RU AUTO+откат); UA только дробь `1.95`→`1,95`; voltage `220` faithful |
| 30 | 2331726062 | SARO RICO рисоварка | RU `квт`→`кВт` + дроби | `квт 1.6`→`кВт 1,6`, `5.1`→`5,1`, `4.2`→`4,2` (RU AUTO+откат); UA только дроби; voltage `220` faithful |
| 31 | 2566572706 | Bartscher А150513 рисоварка | 🔴 | полный RU перевод тег-в-tag (single-line + `</ul>` отдельно); `*`-мультипликатор no-op; voltage `230` faithful |
| 32 | 2567445276 | HURAKAN HKN-SR180 рисоварка | 🔴 | полный RU перевод тег-в-tag (multi-line, `<li>` ×9); глоб.B латин. x; voltage `220` faithful |

---

## Батч SKU 33-40 (40/70)

SKU 33-40 — рисоварки + пароварки Casta + печи на древесном угле (Bartscher / Cuckoo / Casta ×2 / Oztiryakiler / Ефес ×3). **SKU 33** Рисоварка 12 л Bartscher 150529 — 🔴 (RU-поле было полной укр. копией → авто-перевод тег-в-tag, single-line `<ul> … </ul>`). **SKU 34** Рисоварка CUCKOO CR-3021 5,4 л с функцией термоса — без правок (RU самостоятелен корректен). **SKU 35** Пароварка Casta L7DKE400BA (9 корзин) — 🔴 + Назв.мод (RU) UA-leak триплет `(9 кошик)`→`(9 корзин)`. **SKU 36** Пароварка Casta L7DKE800BA (18 корзин) — sibling 35, 🔴 + триплет `(18 кошик)`→`(18 корзин)`. **SKU 37** Печь на древесном угле Oztiryakiler 7890.KYK00.01 — 🔴 + триплет `Піч на деревному вугіллі…`→`Печь на древесном угле…`. **SKU 38** Печь на древесном угле Эфес ПДУ-800 — без правок описания + RU typo `Кмоплектация`→`Комплектация` AUTO+откат. **SKU 39** Печь на древесном угле Эфес ПДУ-900 — sibling 38, без правок описания + тот же RU typo-фикс. **SKU 40** Печь на древесном угле Эфес ПДУ-1000 — sibling 38/39, без правок описания + тот же RU typo-фикс (UA здесь корректно `Комплектація` → доказывает RU-side артефакт). **🔴 в 4 из 8** (SKU 33/35/36/37). Без правок (RU самостоятелен) 4 из 8 (SKU 34 чистый + 38/39/40 описание чистое, только targeted RU typo-фикс). Назв.мод (RU) UA-leak триплет: 3 (SKU 35/36/37). Новых нумерованных Открытых вопросов нет.

### Стандартно применено (locked-паттерны, подтверждения НЕ требуют)
- **🔴 RU=UA полная укр. копия → AUTO полный перевод тег-в-tag:** SKU 33 (Bartscher 150529 — single-line `<ul> <li>…</li> … </ul>` одна строка), SKU 35 (Casta L7DKE400BA — multi-line: строка1 `<h2>…</h2> <ul>` + построчные `<li>` ×14 + `</ul>`), SKU 36 (Casta L7DKE800BA — sibling 35, та же структура), SKU 37 (Oztiryakiler — multi-line `<h2>…</h2> <ul>` + `<li>` ×12 + `</ul>`). House corpus без ё (`котел`/`Объем`/`решетка`); `Об&#39;єм`→`Объем`, `м&#39;яса`→`мяса` (энтити `&#39;` снят); `склянка`→`стакан` (33); `килимок`→`коврик` (33); `іскрогасника`→`искрогаситель`, `ковпак іскрогасника`→`колпак искрогасителя` (37); `&deg;` энтити сохранён (37); хвостовой пробел `+110 °C ` сохранён (35/36); литерал `-` в `В комплекте - щипцы` сохранён (37); `2 решетки x GN 1/1` латинская x → глоб.B в тексте дословно (37); `GN1/1х200`/`GN2/1х200`/`400х700х850`/`800х700х850`/`830х700х1835` х кириллическая — no-op (35/36/37).
- **RU typo `Кмоплектация`→`Комплектация` (очевидная транспозиция `ом`→`мо`, AUTO+откат-нота, RU-only):** SKU 38/39/40 — `<li>Кмоплектация: …</li>`→`<li>Комплектация: …</li>`. Прецедент chunk-014 SKU 1 (`упралнения`→`управления`) / chunk-010 SKU 47 (`Выполена`→`Выполнена`); единогласные siblings (3 из 3). UA-сторона `Кмоплектация` — faithful (UA typos НЕ авто-правятся, locked); **SKU 40 UA корректно `Комплектація`** при RU-typo → подтверждает RU-side машинный артефакт.
- **Реальная `.`-дробь → `,` ОБЕ локали:** НЕТ в батче (все числа целые или уже с запятой: `2,85`/`3,5`/`4,5`/`5,4`/`3,6`/`1,46` — no-op).
- **RU единица `квт`→`кВт`:** НЕТ в батче (везде корректный регистр `кВт`).
- **Назв.мод (RU) UA-leak → триплет:** SKU 35 (`Пароварка Casta L7DKE400BA (9 кошик)`→`(9 корзин)`), SKU 36 (`(18 кошик)`→`(18 корзин)`), SKU 37 (`Піч на деревному вугіллі Oztiryakiler 7890.KYK00.01`→`Печь на древесном угле…`). Переведено собственное украинское содержимое RU-поля; SKU 33/34/38/39/40 — Назв./Назв.мод UA==RU или корректный русский (НЕ leak).
- **Voltage faithful (значение НЕ трогаю — НИКОГДА):** SKU 35/36 `380 В` (в 🔴, метка `напруга`→`напряжение`, значение нетронуто). Остальные SKU батча — voltage в описании нет.
- **Чистые SKU (RU — корректный самостоятельный перевод, без правок описания):** SKU 34 Cuckoo CR-3021 (518661590) — полностью чистый; SKU 38 Эфес ПДУ-800 (1148299767), 39 Эфес ПДУ-900 (1148305861), 40 Эфес ПДУ-1000 (1148316134) — описание чистое (только targeted RU typo-фикс выше). 🔴 — ни в одном из этих.

### Заслуживает внимания (информативно, не блокирует)
- **SKU 33 — UA-русизм `порцій` + META `барчер`.** UA-источник использует `порцій` (русизм вместо `порцій/порцій`-нормы) — RU `порций` faithful (рекуррент SKU 9/23/24). RU/UA META keywords содержат `барчер` (transliteration-typo от Bartscher) — META **always faithful**, не правится.
- **SKU 37 — RU META несёт UA-leak хвост `піч на деревному вугіллі oztiryakiler …`.** META keywords RU-поля содержат непереведённый украинский хвост. META **always faithful** — не правится. Симметрично с UA-стороной.
- **SKU 38 — RU `шампура` (разговорная форма мн.ч.).** В описании RU `шампура` вместо лит. `шампуры` — разговорный/просторечный вариант, faithful, не эскалируется (не ошибка перевода, стилистика источника).
- **SKU 39 — RU META несёт UA-leak хвост.** Аналогично SKU 37 — META faithful, не правится.
- **SKU 40 — UA `Комплектація` корректно, RU `Кмоплектация` с typo.** Расхождение доказывает, что `Кмоплектация` — артефакт RU-side машинной обработки (не наследие UA-источника). RU-фикс применён (см. выше); UA нетронут (корректен). НЕ customer-facing рассинхрон — не Открытый вопрос.

### Сводка батча

| SKU | Артикул | Товар | Тип | Действие |
|----|---------|-------|-----|----------|
| 33 | 475131853 | Рисоварка 12 л Bartscher 150529 | 🔴 | полный RU перевод тег-в-tag (single-line `<ul> … </ul>`); `склянка`→`стакан`, `килимок`→`коврик`; voltage нет |
| 34 | 518661590 | Рисоварка CUCKOO CR-3021 5,4 л с функцией термоса | без правок | RU самостоятелен корректен; реальных дробей нет |
| 35 | 1122652997 | Пароварка Casta L7DKE400BA (9 корзин) | 🔴 + триплет | Назв.мод (RU) `(9 кошик)`→`(9 корзин)`; полный RU перевод (multi-line `<li>` ×14); voltage `380 В` faithful |
| 36 | 1122656036 | Пароварка Casta L7DKE800BA (18 корзин) | 🔴 + триплет | sibling 35; Назв.мод (RU) `(18 кошик)`→`(18 корзин)`; voltage `380 В` faithful |
| 37 | 707420339 | Печь на древесном угле Oztiryakiler 7890.KYK00.01 | 🔴 + триплет | Назв.мод (RU) `Піч…вугіллі…`→`Печь…угле…`; полный RU перевод (multi-line `<li>` ×12); глоб.B латин. x; META UA-leak soft note |
| 38 | 1148299767 | Печь на древесном угле Эфес ПДУ-800 | RU typo-фикс | `Кмоплектация`→`Комплектация` (RU AUTO+откат); UA faithful; RU `шампура` soft note |
| 39 | 1148305861 | Печь на древесном угле Эфес ПДУ-900 | RU typo-фикс | sibling 38; `Кмоплектация`→`Комплектация` (RU AUTO+откат); RU META UA-leak soft note |
| 40 | 1148316134 | Печь на древесном угле Эфес ПДУ-1000 | RU typo-фикс | sibling 38/39; `Кмоплектация`→`Комплектация` (RU AUTO+откат); UA `Комплектація` корректно → доказательство RU-side артефакта |

---

## Батч SKU 41-48 (48/70)

SKU 41-48 — Ефес ПДУ-1200 + дегидраторы (Frosty ×3 / GoodFood / Hendi ×2 / Hurakan). **SKU 41** Печь на древесном угле Эфес ПДУ-1200 — sibling 38/39/40, без правок описания + RU typo `Кмоплектация`→`Комплектация` AUTO+откат (UA здесь корректно `Комплектація` → RU-side артефакт, как SKU 40). **SKU 42** Дегидратор Frosty SS-10H — без правок (RU самостоятелен корректен). **SKU 43** Дегидратор Frosty SS-12H — sibling 42, без правок. **SKU 44** Дегидратор Frosty FFD-32H — без правок (RU слил два UA `<li>` в один — структурное, soft note). **SKU 45** Дегидратор GoodFood WS10 PRO — без правок (richer-RU Название, soft note). **SKU 46** Дегидратор Hendi Profi Line 229026 — 🔴 + Назв.мод (RU) UA-leak триплет `Дегідратор…`→`Дегидратор…`. **SKU 47** Дегидратор HURAKAN HKN-DHD10 — 🔴 + триплет (iframe вербатим). **SKU 48** Дегидратор Hendi 229033 (6 полок) — 🔴 + триплет `Дегідратор Hendi 229033 (6 полиць)`→`Дегидратор Hendi 229033 (6 полок)`. **🔴 в 3 из 8** (SKU 46/47/48). Без правок 5 из 8 (SKU 41 описание чистое + targeted RU typo-фикс; 42/43/44/45 RU самостоятелен корректен). Назв.мод (RU) UA-leak триплет: 3 (SKU 46/47/48). Новых нумерованных Открытых вопросов нет.

### Стандартно применено (locked-паттерны, подтверждения НЕ требуют)
- **🔴 RU=UA полная укр. копия → AUTO полный перевод тег-в-tag:** SKU 46 (Hendi Profi Line 229026 — multi-line: строка1 `<h2>…</h2> <ul>` + построчные `<li>` ×10 + строка `</ul> <p> </p>`), SKU 47 (Hurakan HKN-DHD10 — multi-line + `<li>` ×11 + `</ul> <p><iframe …></iframe> </p>`, iframe сохранён ВЕРБАТИМ + хвостовой пробел ` </p>`), SKU 48 (Hendi 229033 — multi-line + `<li>` ×11 + `</ul> <p> </p>`). `Сушарка/сушарка`→`Сушилка/сушилка` (47/48); `призначений`→`предназначен`, `сушіння`→`сушки` (46/48); `тощо`→`и т.д.` (47); `м&#39;яса`→`мяса` (энтити `&#39;` снят, 46/47/48); house corpus без ё `решеток` (47); `&deg;` энтити сохранён (все 3); `+30...+70` диапазон сохранён (47); `42dB` дословно (47); `15 год`→`15 ч` (47); UA-метка `Напруга`→RU `Напряжение` (47), `Підключення`→`Подключение` (48); `395x395`/`420x550x380`/`396x396`/`305x330` латинская x → глоб.B в тексте дословно; `418х535х430`/`340х450х311` х кириллическая — no-op.
- **RU typo `Кмоплектация`→`Комплектация` (очевидная транспозиция `ом`→`мо`, AUTO+откат-нота, RU-only):** SKU 41 — `<li>Кмоплектация: …</li>`→`<li>Комплектация: …</li>`. 4-й sibling серии Эфес ПДУ (38/39/40/41), единогласно; прецедент chunk-014 SKU 1 (`упралнения`→`управления`) / chunk-010 SKU 47 (`Выполена`→`Выполнена`). **UA здесь корректно `Комплектація`** (как SKU 40) → подтверждает RU-side машинный артефакт; UA нетронут (корректен, locked).
- **Реальная `.`-дробь → `,` ОБЕ локали:** НЕТ в батче (`3,5` уже запятая — no-op; остальные числа целые).
- **RU единица `квт`→`кВт`:** НЕТ в батче (везде корректный регистр `кВт`/`Вт`).
- **Назв.мод (RU) UA-leak → триплет:** SKU 46 (`Дегідратор Hendi Profi Line 229026`→`Дегидратор Hendi Profi Line 229026`), SKU 47 (`Дегідратор HURAKAN HKN-DHD10`→`Дегидратор HURAKAN HKN-DHD10`), SKU 48 (`Дегідратор Hendi 229033 (6 полиць)`→`Дегидратор Hendi 229033 (6 полок)`). Переведено собственное украинское содержимое RU-поля; SKU 41/42/43/44/45 — Назв./Назв.мод UA==RU-эквив или корректный/richer русский (НЕ leak).
- **Voltage faithful (значение НЕ трогаю — НИКОГДА):** SKU 45 `220 В` (RU уже `Напряжение: 220 В`), SKU 47 `220 В` (в 🔴, метка `Напруга`→`Напряжение`, значение нетронуто), SKU 48 `220 В` (в 🔴, метка `Підключення`→`Подключение`, значение нетронуто). SKU 41/42/43/44/46 — voltage в описании нет.
- **Чистые SKU (RU — корректный самостоятельный перевод, без правок описания):** SKU 41 Эфес ПДУ-1200 (1148323842) — описание чистое (только targeted RU typo-фикс выше); SKU 42 Frosty SS-10H (1910412856), 43 Frosty SS-12H (1910421989), 44 Frosty FFD-32H (2269474683), 45 GoodFood WS10 PRO (619259775) — RU самостоятелен корректен. 🔴 — ни в одном из этих.

### Заслуживает внимания (информативно, не блокирует)
- **SKU 42/43 — RU `од` / UA `од` (серия Frosty SS, source-артефакт).** RU `Регулировка температуры од +30&deg;C до +90&deg;C` — `од` следовало бы `от`; UA-источник сам содержит `од` (следовало бы `від`). Рекуррентный артефакт серии Frosty SS (sibling 42→43, тот же source). **НЕ авто-правится:** это не intra-Russian-word transposition (как `Кмоплектация`), а UA-source preposition-leak без установленного кросс-чанк прецедента — locked-консерватизм. RU `од`→`от` + UA `од`→`від` оба faithful. Не нумерованный (word-typo, не число/код).
- **SKU 44 — RU слил два UA `<li>` в один (структурное).** UA имеет 19 `<li>` (отдельные `<li>В комплекті піддон для крихт та води</li>` + `<li>Матеріал: корпус, полиці…</li>`); RU объединил в один `<li>В комплекте поддон для крошек и воды Материал: корпус, полки…</li>` (18 `<li>`). RU-faithful структурное расхождение — НЕ авто-нормализую, не нумерованный (не число/код).
- **SKU 45 — richer-RU Название + img self-close.** RU Назв./Назв.мод `Сушка для пищевых продуктов (дегидратор) GoodFood WS10 PRO` богаче UA `Дегідратор GoodFood WS10 PRO` — richer-RU faithful, НЕ leak. `<img …>` UA без самозакрытия `>` vs RU самозакрыт ` />` — HTML faithful, не нормализую.
- **SKU 46/47/48 — richer-RU Название + META sibling-leak / UA-leak хвост.** RU Название `Сушилка для пищевых продуктов (дегидратор)…` богаче UA `Дегідратор…` (46/48) — richer-RU faithful. RU/UA META keywords несут mixed sibling-leak `goodfood ws10 pro` (46/47/48) + UA-leak хвост `дегідратор/дегідратори` (47/48) — META **always faithful**, не правится.

### Сводка батча

| SKU | Артикул | Товар | Тип | Действие |
|----|---------|-------|-----|----------|
| 41 | 1148323842 | Печь на древесном угле Эфес ПДУ-1200 | RU typo-фикс | sibling 38/39/40; `Кмоплектация`→`Комплектация` (RU AUTO+откат); UA `Комплектація` корректно → RU-side артефакт; описание чистое |
| 42 | 1910412856 | Дегидратор Frosty SS-10H | без правок | RU самостоятелен корректен; soft note RU `од`/UA `од` (Frosty SS source-артефакт, НЕ авто) |
| 43 | 1910421989 | Дегидратор Frosty SS-12H | без правок | sibling 42; RU самостоятелен корректен; тот же soft note `од` |
| 44 | 2269474683 | Дегидратор Frosty FFD-32H | без правок | RU самостоятелен; soft note: RU слил два UA `<li>` в один (структурное faithful) |
| 45 | 619259775 | Дегидратор GoodFood WS10 PRO | без правок | RU самостоятелен; soft note: richer-RU Название + img self-close faithful |
| 46 | 631396372 | Дегидратор Hendi Profi Line 229026 | 🔴 + триплет | Назв.мод (RU) `Дегідратор…`→`Дегидратор…`; полный RU перевод (multi-line `<li>` ×10); глоб.B латин. x; META soft note |
| 47 | 705871662 | Дегидратор HURAKAN HKN-DHD10 | 🔴 + триплет | Назв.мод (RU) `Дегідратор…`→`Дегидратор…`; полный RU перевод (multi-line `<li>` ×11, iframe вербатим); voltage `220 В` faithful |
| 48 | 942243242 | Дегидратор Hendi 229033 (6 полок) | 🔴 + триплет | Назв.мод (RU) `Дегідратор…(6 полиць)`→`Дегидратор…(6 полок)`; полный RU перевод (multi-line `<li>` ×11); voltage `220 В` faithful |

---

## Батч SKU 49-56 (56/70)

SKU 49-56 — дегидраторы (REEDNEE FK03 / Hendi 229064 / Frosty FFD-10H/12H/16H/20H / GGM GASTRO DGHB16/DGHB20). **SKU 49** Дегидратор REEDNEE FK03 — 🔴 + Назв.мод (RU) UA-leak триплет `Дегідратор REEDNEE FK03`→`Дегидратор REEDNEE FK03`. **SKU 50** Дегидратор Hendi 229064 — 🔴 + триплет `Дегідратор Hendi 229064`→`Дегидратор Hendi 229064` (richer-RU Название, soft note). **SKU 51** Дегидратор Frosty FFD-10H — 🔴 + триплет + реальная дробь `10.60`→`10,60` (обе локали). **SKU 52** Дегидратор Frosty FFD-12H — 🔴 + триплет (sibling 51; `20.00` артефакт no-op). **SKU 53** Дегидратор Frosty FFD-16H — 🔴 + триплет (sibling 51/52). **SKU 54** Дегидратор Frosty FFD-20H — 🔴 + триплет (sibling 51/52/53; `2,00` артефакт no-op). **SKU 55** Дегидратор GGM GASTRO DGHB16 — без правок (RU самостоятелен корректен чистый). **SKU 56** Дегидратор GGM GASTRO DGHB20 на 20 полок — без правок описания + реальная дробь `33.5`→`33,5` (обе локали). **🔴 в 6 из 8** (SKU 49/50/51/52/53/54). Без правок 2 из 8 (SKU 55 чистый; SKU 56 описание чистое + targeted дробь-фикс). Назв.мод (RU) UA-leak триплет: 6 (SKU 49/50/51/52/53/54). Новых нумерованных Открытых вопросов нет.

### Стандартно применено (locked-паттерны, подтверждения НЕ требуют)
- **🔴 RU=UA полная укр. копия → AUTO полный перевод тег-в-tag:** SKU 49 (REEDNEE FK03 — multi-line: строка1 `<h2>…</h2> <ul>` + построчные `<li>` ×11 + строка `</ul> <p> </p>`), SKU 50 (Hendi 229064 — строка1 `<ul>` + построчные `<li><font>…</font></li>` ×11 + строка `</ul>`), SKU 51/52/54 (Frosty FFD-10H/12H/20H — multi-line `<p>…</p> <p><strong>…</strong></p> <ul>` + `<li>` ×14 + секции `В комплекте:`/`Материал:`), SKU 53 (Frosty FFD-16H — та же без секций тех.дані/Розміри/Вага, `<li>` ×11 в 1-м ul). `Дегідратор`→`Дегидратор`, `призначений`→`предназначен`, `сушіння`→`сушки` (49); `м'яса`(литерал)/`м&#39;яса`/`м&rsquo;яса`→`мяса` (апостроф/энтити снят, 49/50/51/52/53/54); `тощо`→`и т.д.` (50); `&deg;` энтити + нестандартный спейсинг `&deg; C` сохранён (50); `°C` литерал (49) faithful; `30˚С`/`5˚С`/`+30˚С`/`+90˚С` символ `˚` (U+02DA) + кириллическая `С` сохранён (51-54); house corpus без ё `Оснащен` (50); UA-typo `(дегідрататор)`→корректный RU `(дегидратор)` (51-54); `енергозабезпечення`→faithful `энергообеспечение` (51-54); `305x330` (50) / `345мм x 395мм x 430мм` (51) / `470мм x 670мм x 550мм` (52) / `465мм x 570мм x 820мм` (54) латинская x → глоб.B в тексте дословно; `375х390`/`370х470х550` (49) / `345х450х315` (50) / `400 мм х 380`/`8 х 8`/`8 мм х 8 мм` (51-54) х кириллическая — no-op; `*` множитель в `Размеры (Д * Ш * В)` сохранён (51/52/54); хвостовые пробелы внутри `<li>`/`<font>` сохранены (49/50).
- **Реальная `.`-дробь → `,` ОБЕ локали:** SKU 51 `Вес: 10.60`→`10,60` (UA `Вага: 10.60`→`10,60` + в RU-переводе значение уже `Вес: 10,60`), SKU 56 `33.5`→`33,5` (UA `Вага, кг 33.5`→`33,5` + RU `Вес, кг 33.5`→`33,5`). Артефакт/уже-запятая no-op (НЕ дробь, сохранены): SKU 52 `20.00`, 54 `2,00`, 51 `0,80`, 52 `0,90`, 49 `0,5`.
- **RU единица `квт`→`кВт`:** НЕТ в батче (везде корректный регистр `кВт`/`Вт`).
- **Назв.мод (RU) UA-leak → триплет:** SKU 49 (`Дегідратор REEDNEE FK03`→`Дегидратор REEDNEE FK03`), 50 (`Дегідратор Hendi 229064`→`Дегидратор Hendi 229064`), 51 (`Дегідратор Frosty FFD-10H`→`Дегидратор Frosty FFD-10H`), 52 (`Дегідратор Frosty FFD-12H`→`Дегидратор Frosty FFD-12H`), 53 (`Дегідратор Frosty FFD-16H`→`Дегидратор Frosty FFD-16H`), 54 (`Дегідратор Frosty FFD-20H`→`Дегидратор Frosty FFD-20H`). Переведено собственное украинское содержимое RU-поля; SKU 55/56 — Назв./Назв.мод корректный русский (НЕ leak).
- **Voltage faithful (значение НЕ трогаю — НИКОГДА):** SKU 51/52/54 `220В` (в 🔴, UA-метка `Тех. дані`/`тех. дані`→RU `Тех. данные`/`тех. данные`, значение нетронуто), SKU 55/56 `230V / 50 Hz` (RU самостоятелен — `Электрическое подключение: 230V / 50 Hz` faithful; SKU 56 RU с хвостовым пробелом `230V / 50 Hz ` faithful). SKU 49/50/53 — voltage в описании нет.
- **Чистые SKU (RU — корректный самостоятельный перевод, без правок описания):** SKU 55 GGM GASTRO DGHB16 (2127234603) — RU самостоятелен корректен, описание чистое; SKU 56 GGM GASTRO DGHB20 (2127243834) — описание чистое (только targeted дробь-фикс `33.5`→`33,5` обе локали). 🔴 — ни в одном из этих.

### Заслуживает внимания (информативно, не блокирует)
- **SKU 51-56 — UA рекуррентный typo `(дегідрататор)` (должно `дегідратор`).** Source UA содержит лишний слог. **НЕ авто-правится:** UA-source typo (не intra-Russian transposition, без кросс-чанк прецедента для UA-стороны) — locked-консерватизм, UA-faithful. RU рендерит корректно `(дегидратор)` (в 🔴-переводах 51-54 переведено правильно; в самостоятельных RU 55/56 уже корректно). Не нумерованный (word-typo, не число/код).
- **SKU 52/53/54 — UA typo `платик` (должно `пластик`).** Source UA в секции `Матеріал` (ручки для переноски). UA-faithful, НЕ авто (UA-source word-typo). RU корректно `пластик`. Не нумерованный.
- **SKU 51-54 — source-семантика `енергозабезпечення`.** UA-источник: «двостінний корпус забезпечує енергозабезпечення» (семантически странно — вероятно «енергозбереження»). Переведено faithful как `энергообеспечение` (source-preserved, без флага — не наша интерпретация source-смысла). Не нумерованный.
- **SKU 50 — richer-RU Название vs UA `Дегідратор…`.** RU Назв. `Сушилка для пищевых продуктов (дегидратор) Hendi 229064` богаче UA `Дегідратор Hendi 229064` — richer-RU faithful (Назв.мод (RU) нёс укр. — триплет применён). НЕ leak.
- **SKU 56 — RU `230V / 50 Hz ` хвостовой пробел.** RU-поле самостоятельно содержит хвостовой пробел в значении voltage (vs UA без) — HTML/строка faithful, не нормализую (voltage значение — **НИКОГДА**).
- **META always faithful.** RU/UA META keywords несут mixed sibling-leak (`goodfood ws10 pro` / `hendi 229026` / `ewt inox fk02`) + UA-leak хвост — META **always faithful**, не правится.

### Сводка батча

| SKU | Артикул | Товар | Тип | Действие |
|----|---------|-------|-----|----------|
| 49 | 1439020669 | Дегидратор REEDNEE FK03 | 🔴 + триплет | Назв.мод (RU) `Дегідратор…`→`Дегидратор…`; полный RU перевод (multi-line `<h2>…</h2> <ul>` + `<li>` ×11); `м'яса`→`мяса`; `°C` литерал faithful |
| 50 | 1466724410 | Дегидратор Hendi 229064 | 🔴 + триплет | Назв.мод (RU) `Дегідратор…`→`Дегидратор…`; полный RU перевод (`<li><font>…</font></li>` ×11); `&deg;` сохр; `305x330` глоб.B; richer-RU Название soft note |
| 51 | 2087849623 | Дегидратор Frosty FFD-10H | 🔴 + триплет + дробь | Назв.мод (RU) `Дегідратор…`→`Дегидратор…`; полный RU перевод (`<li>` ×14 + секции); реальная дробь `10.60`→`10,60` ОБЕ локали; voltage `220В` faithful |
| 52 | 2087858192 | Дегидратор Frosty FFD-12H | 🔴 + триплет | sibling 51; Назв.мод (RU) триплет; полный RU перевод; `20.00` артефакт no-op; voltage `220В` faithful; UA typo `платик` soft note |
| 53 | 2087859946 | Дегидратор Frosty FFD-16H | 🔴 + триплет | sibling 51/52; Назв.мод (RU) триплет; полный RU перевод (`<li>` ×11, без тех.дані/Розміри/Вага); voltage нет |
| 54 | 2087861824 | Дегидратор Frosty FFD-20H | 🔴 + триплет | sibling 51/52/53; Назв.мод (RU) триплет; полный RU перевод; `2,00` артефакт no-op; voltage `220В` faithful |
| 55 | 2127234603 | Дегидратор GGM GASTRO DGHB16 | без правок | RU самостоятелен корректен чистый; voltage `230V / 50 Hz` faithful; UA `(дегідрататор)` soft note |
| 56 | 2127243834 | Дегидратор GGM GASTRO DGHB20 на 20 полок | дробь-фикс | RU самостоятелен; описание чистое + реальная дробь `33.5`→`33,5` ОБЕ локали; RU хвост.пробел voltage faithful |

---

## Батч SKU 57-64 (64/70)

SKU 57-64 — дегидраторы (GGM GASTRO DGHB24 / GoodFood WS8PRO / Frosty FFD-40H / Hurakan HKN-DHD10G/16G/12G / GGM GASTRO DGHB10 / GoodFood WS12PRO). **SKU 57** Дегидратор GGM GASTRO DGHB24 на 24 уровня — без правок описания + Назв.мод (RU) UA-leak триплет `на 24 рівня`→`на 24 уровня` (**НЕ 🔴** — RU описание самостоятельно корректно). **SKU 58** Дегидратор GoodFood WS8PRO — без правок (RU самостоятелен корректен). **SKU 59** Дегидратор Frosty FFD-40H — 🔴 + Назв.мод (RU) UA-leak триплет `Дегідратор Frosty FFD-40H`→`Дегидратор Frosty FFD-40H`. **SKU 60** Дегидратор HURAKAN HKN-DHD10G — без правок (RU самостоятелен корректен). **SKU 61** Дегидратор HURAKAN HKN-DHD16G — без правок (sibling 60, `16` уровней). **SKU 62** Дегидратор HURAKAN HKN-DHD12G — без правок (sibling 60/61, `12` уровней). **SKU 63** Дегидратор GGM GASTRO DGHB10 — без правок (RU самостоятелен корректен). **SKU 64** Дегидратор GoodFood WS12PRO — без правок (sibling 58). **🔴 в 1 из 8** (SKU 59). Без правок (RU самостоятелен корректен) 6 из 8 (SKU 58/60/61/62/63/64); SKU 57 — без правок описания + триплет. Назв.мод (RU) UA-leak триплет: 2 (SKU 57 частичный без 🔴, SKU 59 с 🔴). Новых нумерованных Открытых вопросов нет.

### Стандартно применено (locked-паттерны, подтверждения НЕ требуют)
- **🔴 RU=UA полная укр. копия → AUTO полный перевод тег-в-tag:** SKU 59 (Frosty FFD-40H — multi-line: строка1 `<h2>…</h2> <ul>` + построчные `<li>` ×18 + строка `</ul> <p> </p>`). `Сушарка`→`Сушилка`, `використовується`→`используется`, `прискореного сушіння`→`ускоренной сушки`, `м&#39;яса`→`мяса` (энтити апостроф снят), `(дегідратор)`→`(дегидратор)` (здесь UA корректно `дегідратор`, не typo); `год`→`ч`, `хв`→`мин`; `˚` (U+02DA) + кириллическая `С` сохранён (`+30˚С`/`+90˚С`/`5˚С`); `400х380`/`8х8`/`465х570х1616` х кириллическая — no-op; целые числа; voltage в описании нет.
- **Реальная `.`-дробь → `,` ОБЕ локали:** НЕТ в батче. Все веса уже запятая (`8,10` / `9,9` / `9,30` / `11,40` / `0,8`) либо целые — no-op.
- **RU единица `квт`→`кВт`:** НЕТ в батче (везде корректный регистр `кВт`/`Вт`).
- **Назв.мод (RU) UA-leak → триплет:** SKU 57 (`Дегидратор GGM GASTRO DGHB24 на 24 рівня`→`…на 24 уровня` — частичный укр. хвост `рівня`; описание самостоятельно корректно, **НЕ 🔴**), SKU 59 (`Дегідратор Frosty FFD-40H`→`Дегидратор Frosty FFD-40H` — с 🔴). Переведено собственное украинское содержимое RU-поля; SKU 58/60/61/62/63/64 — Назв./Назв.мод корректный русский (НЕ leak; SKU 60 UA было ALLCAPS `ДЕГІДРАТОР`, RU нормализован — не leak).
- **Voltage faithful (значение НЕ трогаю — НИКОГДА):** SKU 58 `220В`, SKU 60/61/62 `0,8 кВт, 220 В`, SKU 64 `220В`, SKU 57/63 `230V / 50 Hz` (SKU 57 UA с хвостовым пробелом `230V / 50 Hz ` vs RU `230V / 50 Hz` faithful). SKU 59 — voltage в описании нет.
- **Latin x → глоб.B (в тексте дословно):** SKU 57 `400x380`+`Ш x Г x В`+`625 мм x 525 мм x 885 мм`, SKU 60/61/62 `290x290`, SKU 63 `300x280`+`Ш x Г x В`+`345 мм x 393 мм x 425 мм`. Кириллическая х no-op: SKU 58/59/60/61/62/64 (габариты). `&times;` энтити в габаритах (SKU 58 `460&times;340&times;320мм` / SKU 64 `420&times;340&times;450мм`) — HTML-сущность, faithful (не латин/кир. x).

### Заслуживает внимания (информативно, не блокирует)
- **SKU 57/63 — UA рекуррентный typo `(дегідрататор)` (должно `дегідратор`).** Source UA содержит лишний слог. **НЕ авто-правится:** UA-source typo (не intra-Russian transposition) — locked-консерватизм, UA-faithful. RU рендерит корректно `(дегидратор)`. Не нумерованный (word-typo, не число/код).
- **SKU 63 — UA typo `свыжих` (должно `свіжих`).** Source UA word-typo. **НЕ авто** (UA-faithful). RU корректно `свежих`. Не нумерованный.
- **SKU 60/61/62 — UA `+30/+90 &deg;С` vs RU `+30/90 &deg;С`.** Значения 30/90 совпадают, отличается лишь знак `+` у второго операнда. RU faithful, не нумерованный (значения идентичны, не value/код-desync).
- **SKU 63 — UA `Відстань між рівнями: 35 мм` vs RU `Расстояние между полками: 35 мм`.** Non-number wording-разница, значение 35 мм совпадает. RU faithful, не нумерованный.
- **SKU 58/64 — UA/RU диапазон температур.** SKU 58 UA `35&ndash;75 &deg;С` vs RU `35-75 &deg;С` (`&ndash;`/`-` дословно), SKU 64 `35-90 &deg;С` (UA=RU) — значения совпадают, faithful.
- **META always faithful.** RU/UA META keywords несут mixed sibling-leak + UA-leak хвост — META **always faithful**, не правится.

### Сводка батча

| SKU | Артикул | Товар | Тип | Действие |
|----|---------|-------|-----|----------|
| 57 | 2127248765 | Дегидратор GGM GASTRO DGHB24 на 24 уровня | триплет (без 🔴) | Назв.мод (RU) `на 24 рівня`→`на 24 уровня`; RU описание самостоятельно корректно; глоб.B латин. x; UA `(дегідрататор)` soft note |
| 58 | 2172851776 | Дегидратор GoodFood WS8PRO | без правок | RU самостоятелен корректен; `&times;` энтити габариты faithful; `&ndash;` диапазон faithful; voltage `220В` faithful |
| 59 | 2269483280 | Дегидратор Frosty FFD-40H | 🔴 + триплет | Назв.мод (RU) `Дегідратор…`→`Дегидратор…`; полный RU перевод (multi-line `<li>` ×18); `Сушарка`→`Сушилка`; `м&#39;яса`→`мяса`; `˚`(U+02DA)+кир.С faithful |
| 60 | 2784075270 | Дегидратор HURAKAN HKN-DHD10G | без правок | RU самостоятелен корректен; `290x290` глоб.B; `0,8 кВт, 220 В` faithful; `+30/+90` vs `+30/90` soft note |
| 61 | 2784194353 | Дегидратор HURAKAN HKN-DHD16G | без правок | sibling 60 (`16` уровней); `290x290` глоб.B; voltage `220 В` faithful; тот же `+30/90` soft note |
| 62 | 2784196988 | Дегидратор HURAKAN HKN-DHD12G | без правок | sibling 60/61 (`12` уровней); `290x290` глоб.B; voltage `220 В` faithful; тот же `+30/90` soft note |
| 63 | 2127221592 | Дегидратор GGM GASTRO DGHB10 | без правок | RU самостоятелен корректен; глоб.B латин. x; voltage `230V / 50 Hz` faithful; UA typo `свыжих`/`(дегідрататор)` soft note |
| 64 | 2172871967 | Дегидратор GoodFood WS12PRO | без правок | sibling 58; `&times;` энтити габариты faithful; `35-90 &deg;С` faithful; voltage `220В` faithful |

---

## Батч SKU 65-70 (70/70)

SKU 65-70 — финальный батч chunk-014: дегидраторы Saro VEGGYDRY (65) / GoodFood DVF5 (66) + Apach конвекционные печи AD44D/AD46MV/AD46D (67-69) + пароконвектомат AP5QD (70). **SKU 65** Дегидратор Saro VEGGYDRY (сушка для пищевых продуктов) — без правок (RU самостоятелен корректен). **SKU 66** Дегидратор GoodFood DVF5 — без правок (RU самостоятелен корректен). **SKU 67** Печь конвекционная Apach AD44D электронное управление — 🔴 RU=UA полная укр. копия → авто-перевод тег-в-tag + Назв.мод (RU) UA-leak триплет `Піч конвекційна Apach AD44D електронне керування`→`Печь конвекционная Apach AD44D электронное управление`. **SKU 68** Печь конвекционная Apach AD46MV — без правок (RU самостоятелен корректен). **SKU 69** Печь конвекционная Apach AD46D — без правок (RU самостоятелен корректен; RU `<h4>`→`<p>` нормализация заголовка + RU li-merge — структурный soft note). **SKU 70** Пароконвектомат Apach AP5QD (AP5D) с автомойкой — без правок (RU самостоятелен корректен). **🔴 в 1 из 6** (SKU 67). Без правок (RU самостоятелен корректен) 5 из 6 (SKU 65/66/68/69/70). Назв.мод (RU) UA-leak триплет: 1 (SKU 67, с 🔴). Новых нумерованных Открытых вопросов нет.

### Стандартно применено (locked-паттерны, подтверждения НЕ требуют)
- **🔴 RU=UA полная укр. копия → AUTO полный перевод тег-в-tag:** SKU 67 (Apach AD44D — multi-line: строка1 `<h3>…</h3> <p>…</p> … <ul>` + построчные `<li>` ×8 + строка `</ul> <p>…</p> <p>…</p>`). `Під&#39;єднання`→`Подключение`, `м&#39;яса`→`мяса` (апостроф-энтити снят); `470х340`/`600х660х580` х кириллическая — no-op; `3,2` уже запятая; `280`/`80`/`4` целые; voltage `220/380 В` в описании НЕ тронут (**НИКОГДА**); `&deg;C`/`&ndash;` энтити + emoji `🍴` + код `GN2/3` сохранены.
- **Реальная `.`-дробь → `,` ОБЕ локали:** НЕТ в батче. Все веса/значения уже запятая (`30,5`/`0,5`/`19,5`/`0,42`/`2,6`/`0,3`/`3,2`/`6,2`/`6,7`/`10,8`) либо целые — no-op.
- **RU единица `квт`→`кВт`:** НЕТ в батче (везде корректный регистр `кВт`).
- **Назв.мод (RU) UA-leak → триплет:** SKU 67 (`Піч конвекційна Apach AD44D електронне керування`→`Печь конвекционная Apach AD44D электронное управление` — с 🔴). SKU 65/66/68/69/70 — Назв./Назв.мод корректный русский (НЕ leak).
- **Voltage faithful (значение НЕ трогаю — НИКОГДА):** SKU 65 `220В` (`Напруга`→`Напряжение`), 66 `220В/50Гц` (`Напруга`→`Напряжение`), 67 `220/380 В` (в 🔴, `Під&#39;єднання`→`Подключение`), 68 `380/220 В` (`Під&#39;єднання`→`Подключение к электросети`), 69 `220/380 В` (li-merge, значение не тронуто), 70 `380 В` (`Підключення`→`Подключение к электросети`).
- **Latin x → глоб.B (в тексте дословно):** SKU 66 `280 x 280 x 268` + `295x295x265`. Кириллическая х no-op: SKU 65/67/68/69/70 (габариты).

### Заслуживает внимания (информативно, не блокирует)
- **SKU 66 — UA рекуррентные typo `харчовоого` (должно `харчового`) и `поліпропелену` (должно `поліпропілену`).** UA-source typos. **НЕ авто** (UA-faithful). RU рендерит корректно `пищевого`/`полипропелена`. Не нумерованный (word-typo, не число/код).
- **SKU 69 — RU `<h4>`→`<p>` нормализация заголовка + RU li-merge.** RU самостоятельно нормализует `<h4><strong>Основні переваги…</strong></h4>`(UA) → `<p><strong>Основные преимущества…</strong></p>`; два UA `<li>` (`Потужність 6,7 кВт.` + `Під&#39;єднання до електромережі: 220/380 В`) слиты RU в один `<li>Мощность 6,7кВт.Подключение к электросети: 220/380 В</li>`. RU-faithful структурно, voltage значение НЕ тронуто. Не нумерованный (структурное, не число/код).
- **SKU 70 — UA source-oddity `Температура температури: до 270&deg;C` (вероятно должно `Робоча температура`).** RU faithful `Температура температуры: до 270&deg;C` (source-preserved). **НЕ авто** (UA-faithful). Не нумерованный.
- **SKU 65 — RU word-choice `сушка` (vs дом-корпус `сушилка`).** RU описание самостоятельно корректно (`<h2>Сушка для пищевых продуктов (дегидратор) Saro VEGGYDRY…`). RU-faithful, не флаг (RU независим, НЕ 🔴).
- **SKU 68 — UA `30&deg; &mdash; 280&deg;` vs RU `30 &deg; - 280 &deg;`.** Значения 30/280 совпадают, отличается лишь `&mdash;`/`-` и спейсинг энтити. RU faithful, не нумерованный (значения идентичны).
- **META always faithful.** RU/UA META несут mixed sibling-leak + UA-leak хвост — META **always faithful**, не правится.

### Сводка батча

| SKU | Артикул | Товар | Тип | Действие |
|----|---------|-------|-----|----------|
| 65 | 1143752240 | Дегидратор Saro VEGGYDRY (сушка для пищевых продуктов) | без правок | RU самостоятелен корректен; voltage `220В` faithful; кир.х no-op; RU `сушка` vs дом `сушилка` soft note |
| 66 | 2172876006 | Дегидратор GoodFood DVF5 | без правок | RU самостоятелен; `об&#39;єм`→`объем`; `280 x 280 x 268`/`295x295x265` глоб.B; voltage `220В/50Гц` faithful; UA typo `харчовоого`/`поліпропелену` soft note |
| 67 | 525347674 | Печь конвекционная Apach AD44D электронное управление | 🔴 + триплет | Назв.мод (RU) `Піч…керування`→`Печь…управление`; полный RU перевод (строка1 `<h3>…<ul>` + `<li>` ×8 + `</ul> <p> <p>`); `Під&#39;єднання`→`Подключение`, `м&#39;яса`→`мяса`; voltage `220/380 В` faithful |
| 68 | 525866389 | Печь конвекционная Apach AD46MV | без правок | RU самостоятелен корректен; voltage `380/220 В` faithful; кир.х no-op; `30&deg;&mdash;280&deg;` vs `30 &deg; - 280 &deg;` faithful |
| 69 | 527283030 | Печь конвекционная Apach AD46D | без правок | RU самостоятелен; voltage `220/380 В` faithful; `м&#39;яса`→`мяса`; RU `<h4>`→`<p>` + li-merge soft note |
| 70 | 527390390 | Пароконвектомат Apach AP5QD (AP5D) с автомойкой | без правок | RU самостоятелен корректен; voltage `380 В` faithful; кир.х no-op; UA source-oddity `Температура температури` soft note |

---

## chunk-014 — ЗАКРЫТ (70/70)

**Все 70 SKU обработаны.** Diff: `chunk-014-diff.md` (Status `COMPLETE 70/70`, секция `## chunk-014 — ИТОГ`). Apply key — `Артикул` (scoped per row). **Diff НЕ применяется к live Horoshop без явного go-ahead Yana + safe mode.**

**Итог авто-применённого (locked, подтверждения не требует):**
- 🔴 RU=UA full translate (Описание RU = укр. копия → самостоятельный RU тег-в-tag): SKU 3, 4, 5, 9, 13, 14, 16, 17, 18, 19, 20, 23, 24, 31, 32, 33, 35, 36, 37, 46, 47, 48, 49, 50, 51, 52, 53, 54, 59, 67 (эталона нет → перевод строго по источнику; SKU 5 — 🔴 + дробь `1.5`→`1,5`; 51 — 🔴 + дробь `10.60`→`10,60`).
- Назв.мод (RU) UA-leak → корректное Название (RU) триплетом: SKU 3, 4, 14, 16, 17, 35, 36, 37, 46, 47, 48, 49, 50, 51, 52, 53, 54, 59, 67 (с 🔴), 57 (без 🔴 — частичный укр. хвост `на 24 рівня`→`на 24 уровня`, описание самостоятельно корректно).
- Реальная дробь `.`→`,` (обе локали, вкл. 🔴): SKU 5 `1.5`→`1,5`, 51 `10.60`→`10,60`, 56 `33.5`→`33,5`. Прочие — целые / `.0`-`.00`-артефакты / уже запятая / диапазоны / коды → no-op.
- RU авто-фикс единицы `квт`→`кВт` (рекуррент Saro, AUTO + откат-нота RU-only): SKU 29, 30 (SARO JULIUS/RICO).
- RU авто-фикс intra-Russian typo `Кмоплектация`→`Комплектация` (транспозиция букв, AUTO + откат-нота RU-only, прецедент chunk-010 SKU 47): SKU 38, 39, 40, 41 (Эфес ПДУ-800/900/1000/1200).

**Faithful (locked, не тронуто):** напряжение/voltage везде — значение НИКОГДА (`220 В`/`220В`/`220В/50Гц`/`220/380 В`/`380/220 В`/`380 В`/`230V / 50 Hz`; метки `Напруга`→`Напряжение`, `Підключення`→`Подключение`, `тех. дані`→`тех. данные`); META всегда (укр./рус. слова, sibling-leak, UA-leak хвост); `об&#39;єм`/`Під&#39;єднання`(UA)→`объем`/`Подключение`(RU), `м&#39;яса`/`м&rsquo;яса`→`мяса` — дом-корпус без ё, апостроф-энтити снят; UA-source typos сохранены (`(дегідрататор)` рекуррент, `платик`, `харчовоого`/`поліпропелену`, `свыжих`, source-oddity `Температура температури`); RU-структурные (li-merge / `<h4>`→`<p>` SKU 69) faithful; глоб. форматы A (вес `NN.00`) / B (латинская `x`/`X`→габариты) / C (пробел перед ед.) — закрываются на ОТДЕЛЬНОЙ глобальной сборке, НЕ per-SKU; `&times;`/`&ndash;`/`&mdash;`/`&deg;`/`˚`(U+02DA) энтити/символы источника сохранены; iframe/`<font>`/`<img>` self-close дословно.

**Следующее:** chunk-015 (chunk-001 — SKIP навсегда).

---

## Открытые вопросы chunk-014 (финализированы при закрытии чанка)

1. **SKU 6 (Артикул 2098339490) — рассинхрон model-кода `Moduline CHS082` ↔ `CHS082E`.** UA Название и Название модификации = `Піч низькотемпературна Moduline CHS082 із коптильнею` (без `E`); при этом UA тело описания (`<h2>…Moduline CHS082E статична…`) и ВСЕ RU-поля (Название/Назв.мод/тело) = `Moduline CHS082E` (с `E`). Внутренний рассинхрон model-кода в UA (заголовок vs тело), customer-facing. Числа/код **не тронуты** (locked — решение за тобой; на применение diff не влияет). Аналог chunk-013 #1 (КПЕЕ↔КПЕ заголовок↔тело). Варианты: (а) UA title/Назв.мод → `CHS082E` (выровнять по телу+RU); (б) оставить как есть (возможно две ревизии модели). Реши, как унифицировать.

_chunk-014 закрыт 70/70. Единственный Открытый вопрос #1 (SKU 6 Moduline `CHS082`↔`CHS082E`) — рассинхрон model-кода title↔body внутри UA-источника (RU-поля консистентны `CHS082E`), customer-facing, аналог chunk-013 #1 (КПЕЕ↔КПЕ). По locked-правилу model-коды не правлю автоматически — нужно твоё решение по источнику (выровнять UA по телу+RU `CHS082E` либо оставить как две ревизии модели). На применение diff (🔴/decimal/Назв.мод) НЕ влияет._

---

**Last updated:** 2026-05-17 — **chunk-014 ЗАКРЫТ 70/70.** Финальный батч SKU 65-70: дегидраторы Saro VEGGYDRY (65) / GoodFood DVF5 (66) + Apach конвекционные печи AD44D/AD46MV/AD46D (67-69) + пароконвектомат AP5QD (70). 🔴 RU=UA 1 из 6: SKU 67 Apach AD44D (RU описание = полная украинская копия UA → авто-перевод тег-в-tag, строка1 `<h3>…</h3> <p>…</p> … <ul>` + `<li>` ×8 + строка `</ul> <p>…</p> <p>…</p>`; `Під&#39;єднання`→`Подключение`, `м&#39;яса`→`мяса` энтити снят; `470х340`/`600х660х580` кир.х no-op; `3,2` уже запятая; voltage `220/380 В` НЕ тронут — НИКОГДА) + Назв.мод (RU) UA-leak триплет `Піч конвекційна Apach AD44D електронне керування`→`Печь конвекционная Apach AD44D электронное управление`. Без правок (RU самостоятелен корректен) 5 из 6: SKU 65 Saro VEGGYDRY, 66 GoodFood DVF5, 68 Apach AD46MV, 69 Apach AD46D, 70 Apach AP5QD. Реальной `.`-дроби в батче НЕТ (`30,5`/`0,5`/`19,5`/`0,42`/`2,6`/`0,3`/`3,2`/`6,2`/`6,7`/`10,8` уже запятая, прочие целые — no-op). RU `квт`→`кВт`: нет (везде корректный регистр `кВт`). Voltage НЕ тронут (значение — НИКОГДА): SKU 65 `220В`, 66 `220В/50Гц`, 67 `220/380 В` (в 🔴), 68 `380/220 В`, 69 `220/380 В` (li-merge), 70 `380 В` (метки `Напруга`/`Підключення`→`Напряжение`/`Подключение`). Латин. x → глоб.B: только SKU 66 (`280 x 280 x 268` + `295x295x265`). Кириллическая х no-op: SKU 65/67/68/69/70 (габариты). `&deg;`/`&deg; C`/`&deg;C` энтити все faithful; `&ndash;`/`&mdash;`(UA) vs `-`/`&ndash;`(RU) faithful. Soft notes: SKU 66 UA рекуррентные typo `харчовоого`/`поліпропелену` — UA-faithful НЕ авто (RU корректно `пищевого`/`полипропелена`); SKU 69 RU `<h4>`→`<p>` нормализация заголовка + RU li-merge (`Потужність 6,7 кВт.`+`Під&#39;єднання 220/380 В` → один RU `<li>`) — RU-faithful структурно soft НЕ нумерованный; SKU 70 UA source-oddity `Температура температури` (вероятно `Робоча температура`) — RU faithful НЕ авто; SKU 65 RU `сушка` vs дом-корпус `сушилка` — RU независим faithful; SKU 68 UA `30&deg; &mdash; 280&deg;` vs RU `30 &deg; - 280 &deg;` значения совпадают faithful; META always faithful. Новых нумерованных Открытых вопросов нет (Назв.мод-leak = триплет НЕ customer-facing; li-merge/`<h4>`→`<p>` = структурное soft; UA typos = soft UA-faithful не число/код; customer-facing число/код-рассинхронов в батче нет). **Открытые вопросы chunk-014 финализированы:** #1 SKU 6 Moduline `CHS082`↔`CHS082E` — рассинхрон model-кода title↔body внутри UA-источника (RU-поля консистентны `CHS082E`), customer-facing, аналог chunk-013 #1; решение по источнику за Yana, на применение diff НЕ влияет. Итог авто-применённого по чанку: 🔴 RU=UA full translate SKU 3/4/5/9/13/14/16/17/18/19/20/23/24/31/32/33/35/36/37/46/47/48/49/50/51/52/53/54/59/67; Назв.мод UA-leak→триплет SKU 3/4/14/16/17/35/36/37/46/47/48/49/50/51/52/53/54/57/59/67; реальная дробь `.`→`,` SKU 5 (`1.5`)/51 (`10.60`)/56 (`33.5`); RU `квт`→`кВт` рекуррент Saro SKU 29/30; RU typo `Кмоплектация`→`Комплектация` SKU 38/39/40/41. (Предыдущие батчи: SKU 57-64 — дегидраторы, 🔴 59, без правок 58/60/61/62/63/64, триплеты 57/59. SKU 49-56 — REEDNEE/Hendi/Frosty дегидраторы, 🔴 49/50/51/52/53/54 (+триплеты, +дробь 51), без правок 55/56 (+дробь 56). SKU 41-48 — Эфес ПДУ + Frosty дегидр., 🔴 46/47/48 (+триплеты), без правок 42/43/44/45, typo 41. SKU 33-40 — рисоварки/Casta/Oztiryakiler/Эфес ПДУ, 🔴 33/35/36/37, без правок 34/38/39/40, typo 38/39/40. SKU 25-32 — термосы/рисоварки, 🔴 31/32, без правок остальные, квт→кВт 29/30. SKU 17-24 — пароварки/рисоварки, 🔴 17/18/19/20/23/24. SKU 9-16 — Moduline/рисоварки, 🔴 9/13/14/16, триплеты 14/16, Открытый вопрос #1 SKU 6. SKU 1-8 — Alto-Shaam/Hendi/Moduline, 🔴 3/4/5, триплеты 3/4, дробь 5.) NEXT: chunk-015 (scaffold; chunk-001 — SKIP навсегда).
