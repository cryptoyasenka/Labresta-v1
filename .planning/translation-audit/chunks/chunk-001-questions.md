# Chunk-001 — Open questions for Yana (batch-answer)

**Контекст:** chunk-001 source audit готов (83/83 SKU в `chunk-001-diff.md`, commit `9a4ad78`). Перед генерацией `chunk-001-fixed.xlsx` (LIVE store upload) нужны ответы на 8 вопросов. Все правки уже расписаны в diff с предположительной рекомендацией — Yana подтверди/исправь.

Формат: одной строкой `YES` / `NO` / альтернатива. Можно одним сообщением все 8 ответов.

---

## Q1 — SKU 55 `1316945344` AIRHOT IP3500 — WOK или D-вариант?

Карточка противоречива:
- **Название UA:** `Плита індукційна AIRHOT IP 3500 настольная` (без WOK, с RU-словом `настольная`)
- **Название RU:** `Плита индукционная AIRHOT IP3500 WOK` (с WOK, без `настольная`)
- **META keywords (RU):** `плита вок, ip3500 wok` — подтверждают WOK
- **URL alias:** `plyta-induktsiina-airhot-ip-3500-nastolnaia` (без WOK)
- **Цена:** 112.10 EUR (для сравнения: SKU 54 AIRHOT IP3500 D = 145 EUR — другая модель)

**Гипотеза:** это AIRHOT IP3500 WOK (без D), UA-название пропустило `WOK` и ошибочно содержит RU-слово.

**Что хочу сделать:**
- UA: `Плита індукційна AIRHOT IP3500 WOK настільна`
- RU: `Плита индукционная AIRHOT IP3500 WOK настольная`

**Yana:** WOK подтверждаешь? (Alias менять не буду — Horoshop SEO-риск.)

---

## Q2 — SKU 64 `1559020605` REEDNEE NSA897A — убрать `NC` суффикс?

- **Название UA:** `Плита індукційна REEDNEE NSA897A` (без NC)
- **Название RU:** `Плита индукционная REEDNEE NSA897A NC` (с NC)

META keywords обоих языков — полный cross-card leak с SKU 52 (REEDNEE WOK AMCD506W). Гипотеза: `NC` в RU — копипаста с другой карточки.

**Что хочу сделать:** убрать `NC` из RU (унифицировать с UA).

**Yana:** OK убрать или `NC` — реальный суффикс модели?

---

## Q3 — SKU 67 `1899491933` Silver 2900 LPG — убрать `(балон)` из UA?

- **UA:** `Плита газова SILVER 2900 LPG (балон)`
- **RU:** `Плита газовая SILVER 2900 LPG` (без)
- **Alias:** `plyta-hazova-silver-2900-lpg-balon`

LPG = liquefied petroleum gas = балонний газ по определению. SKU 68 (Silver 2905 LPG) такого маркера не имеет.

**Что хочу сделать:** убрать `(балон)` из UA → `Плита газова SILVER 2900 LPG`.

**Yana:** убрать `(балон)` или это значимый отличительный признак (только балонная версия, а 2905 — пропан-баллон + магистраль)?

---

## Q4 — SKU 72/73 (реальные 73/74) APACH APRI-47P vs APRI-47P/PL — `/PL` отдельный SKU?

- **SKU 72 (real 73)** `2065658777`: Название = `APACH APRI-47P`, Название модификации (RU) содержит суффикс `/PL`. Цена 5992.60 EUR.
- В каталоге APACH: `APRI-47P` (стандарт) и `APRI-47P/PL` (полированная поверхность) — разные SKU.

**Гипотеза:** карточка для модели с полировкой `/PL`, но названия не отражают.

**Что хочу сделать:**
- Если `/PL`: переименовать все 4 поля Названия в `APACH APRI-47P/PL`.
- Если стандартная: убрать `/PL` из RU модификации.

**Yana:** какой это вариант — `/PL` или без?

---

## Q5 — SKU 76 (real 77) `2094261764` FROSTY FSI-2 — макс. нагрузка `0 кг` реальная?

В описании FSI-2 встречается параметр `Максимальная нагрузка на поверхность: 0 кг`. Это явно ошибка ввода — стеклокерамика не имеет 0 кг лимита.

**Yana:** какое реальное значение? У аналогов:
- FROSTY 35-T1 = 25 кг
- AIRHOT IP3500 WOK = 25 кг
- REEDNEE NSA897A = 40 кг

Если не помнишь — оставляем `0 кг`? Или удалить эту строчку из описания совсем?

---

## Q6 — SKU 78 (real 79) `2122405914` Hendi 239414 — термин `духовки` для индукционной плиты?

Карточка Hendi 239414 — настольная индукционная плита (без духовки). В META keywords и описании встречается слово `духовка` (термин предназначен для печей с камерой выпечки).

**Что хочу сделать:** удалить `духовки/духовка` из keywords и описания, заменить на `плита/конфорка`.

**Yana:** OK удалить, или это намеренный SEO-keyword (хотя продукт не имеет духовки)?

---

## Q7 — Формат `м3` ASCII vs `м³` unicode

Текущий каталог использует ASCII `м3/год`, `м3/ч`. Unicode `м³` рендерится правильно в большинстве браузеров, но Horoshop+CMS+экспорт может сломаться.

**Что я делаю:** оставляю `м3/ч` ASCII (по умолчанию consistent с источником).

**Yana:** OK или мигрировать на `м³`? (Я бы оставила ASCII — меньше риска.)

---

## Q8 — Символ умножения в размерах: `*` → `×`?

В части карточек размеры написаны через звёздочку (`300*300 мм`), в большинстве — через `х` кириллицу (`300х300 мм`). Иногда встречается ASCII `x` (`300x300`).

**Что я делаю:** унифицирую все на `х` кириллицу (текущий доминирующий вариант, 80% карточек).

**Yana:** OK или предпочитаешь typographic `×` (U+00D7)? (Кириллица `х` безопаснее — точно отрисуется в любом фоне.)

---

## После твоих ответов

Когда дашь ответы — собираю `chunk-001-fixed.xlsx` через openpyxl-скрипт (читает `chunk-001.xlsx`, применяет правки по Артикулу, пишет fixed.xlsx). До этого момента chunk-001 в статусе `awaiting_answers`.

Параллельно — продолжаю аудит chunk-002+ в ночном режиме.
