# chunk-013 translation diff (61 SKU — проф. кухонное оборудование, Ефес-dominant)

**Source:** `horoshop-export 13.05.26.xlsx` chunk-013 (61 SKU, продолжение chunk-012)
**Apply key:** `Артикул` (scoped per row)
**Status:** IN PROGRESS 24/61

**Brands:** Ефес (22), Hendi (8), Sirman (8), Ozti (6), FROSTY (4), АТА (3), Kogast (3), Орест (2), Bertos (2), Hurakan (2), Alto-Shaam (1)

**Standing rules** (inherited from chunk-001 — chunk-012):
- UA term `жаркова`/`жарильна`/`смарочна` → `жарочна` (locked Yana 2026-05-14; registry `GLOBAL-SWEEP-zharkova.md` — аудит per-SKU, добавлять в реестр при встрече)
- RU machine artifact `жареная поверхность` → `жарочная поверхность`; `жареная для жарки` → `жарочная` (drop дубля)
- UA `Контейнер для сбору жиру` → `Контейнер для збирання жиру` (ONLY предлог); RU `Контейнер по сбору жира` → `для сбора`
- UA/RU `мм :` → `мм:`
- RU `от NN до NNN&deg;C` → `от NN&deg;C до NNN&deg;C` ТОЛЬКО если UA имеет оба &deg;C и значения совпадают; иначе FLAG
- decimal `N.N` → `N,N` обе локали (реальные дроби: меняется разделитель, точность сохранена; integers/already-comma — no-op)
- RU-leak UA `і`→`и`; 🔴 RU=UA полная укр. копия → AUTO full RU translate (структура HTML tag-в-tag, ✅ АВТО)
- Название модификации (RU) на украинском → AUTO перевод по Название (RU)
- Очевидный typo (удвоение/выпадение слога) → AUTO; однозначная машинная мистрансляция → AUTO + note
- UA→RU операторские термины: `Деко`→`Противень`, `Деко-решітка`→`Противень-решётка`, `Піч НВЧ`→`Печь СВЧ` (memory `feedback_labresta_ua_ru_translation_rules`)
- **4 формат-политики A/B/C → GLOBAL assembly-time pass — НЕ per-SKU diff, НЕ FLAG.** A вес `NN.00`→`NN кг.`/целое · B латинская `x`→кириллическая `х` в габаритах · C пробел перед `мм/см` (вкл. UA `18кВт`/`27кВт` слитно). Спека `.planning/translation-audit/GLOBAL-SWEEP-format.md`; применяется `apply_chunk_diff.py` при сборке master-xlsx ко всем чанкам. **В chunk-013 вес `NN.00` / латинскую `x` / `NNмм` НЕ диффать и НЕ флагать — закрыто глобально.**
- **F (структурный, per-SKU):** HTML `<br />`-склейка двух+ характеристик в одном `<li>` → раздельные `<li>` зеркально чистой стороне. Применять при встрече (ретро chunks 005-008 закрыт).
- FLAG (НЕ авто, → MANUAL-REVIEW русский): T5 surface conflict (текст/код vs spec); RU temp values ≠ UA; spec single vs text dual zone; UA title без дескриптора что есть в body+RU; source data error UA=RU → soft note / Открытые вопросы. **Вес `NN.00` БОЛЬШЕ НЕ флагается (policy A глобально).**

---

## SKU 1/61 — Эфес КЭ-300 котёл пищеварочный пароводяной 300 л (Артикул 1141037624) — без правок (RU корректен)

Прямое продолжение суб-серии Эфес КЭ пароводяных котлов с хвоста chunk-012 (SKU 56-61 КЭ-60…250). RU везде самостоятельный корректный перевод (UA `<h2>Котел харчовий…застосовується` → RU `<h2>Котел пищеварочный…применяется`), НЕ 🔴. Назв./Назв.мод (RU) `Котел пищеварочный Эфес КЭ-300 (пароводяной)` корректны (КЕ→КЭ транслит, `+пищеварочный` обогащение faithful — паттерн SKU 1). Реальных `.`-дробей нет: `0,005` МПа / `0,34` кВт·ч уже запятая, габариты `1550х800х850` (кир. х, целые), `300 л`/`30 кВт` целые — no-op (locked). Напряжение `380 В` faithful (НИКОГДА). Soft note: UA `Потужність 30кВт` слитно (формат-класс C — на ОТДЕЛЬНОЙ глобальной сборке, НЕ per-SKU; RU `Мощность 30 кВт` корректен, значение не тронуто). `Корисний об&#39;єм`(UA)→`Полезный объем`(RU) норм Эфес-шаблон; `&deg; С`/`&deg;С` источника сохранены.

*(scoped к row Артикул=1141037624)*

---

## SKU 2/61 — Эфес КЭ-400 котёл пищеварочный пароводяной 400 л (Артикул 1141039601) — без правок (RU корректен)

Sibling SKU 1 (КЭ-400, пароводяной). RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) корректны. Реальных `.`-дробей нет: `0,005`/`0,54` уже запятая, габариты `1600х900х900` (кир. х, целые), `400 л`/`36 кВт` целые — no-op. Напряжение `380 В` faithful (НИКОГДА). Soft note: UA `Потужність 36кВт` слитно (формат-класс C глоб., RU `36 кВт` корректен, значение не тронуто). `Корисний об&#39;єм`(UA)→`Полезный объем`(RU) шаблон; `&deg; С`/`&deg;С` сохранены.

*(scoped к row Артикул=1141039601)*

---

## SKU 3/61 — Эфес КПЭЭ-60М котёл пищеварочный масляный нагрев 60 л (Артикул 1145549505) — без правок (RU корректен)

Смена суб-серии: Эфес КПЕЕ-М (масляное/олійне нагрівання). RU самостоятельный корректный перевод (UA `Котел харчоварильний…застосовується` → RU `Котел пищеварочный…применяется`), НЕ 🔴. Назв (RU) `Котел Эфес КПЭЭ-60М (масляный нагрев)` — корректный перевод UA `Котел Ефес КПЕЕ-60М (олійне нагрівання)` (олійне→масляный, нагрівання→нагрев, КПЕЕ→КПЭЭ транслит); без `пищеварочный` — faithful вариация источника (Назв SKU 4-7 его имеют; обогащение Названия — faithful, не авто, не UA-leak). Реальных `.`-дробей нет: `7,5 (3х2,5) кВт` уже запятая, `х`-множитель кир., `21 л`/`60 л`/габариты `700х900` целые — no-op. Напряжение `380 В` faithful (НИКОГДА). `Корисний об&#39;єм`(UA)→`Полезный объем`(RU) шаблон. **🔶 См. Открытый вопрос chunk-013 #1 (батч-ур., SKU 3-8): модель-код Названия/Назв.мод `КПЕЕ-60М`/`КПЭЭ-60М` (двойное Е/Э) ≠ заголовок h2 + текст описания `КПЕ-60М`/`КПЭ-60М` (одиночное Е/Э); расхождение симметрично UA↔RU — рассинхрон title↔body внутри источника, не локализационная ошибка. Коды НЕ тронуты (locked); решение по источнику за Yana; НЕ блокирует.**

*(scoped к row Артикул=1145549505)*

---

## SKU 4/61 — Эфес КПЭЭ-100М котёл пищеварочный масляный нагрев 100 л (Артикул 1145554349) — без правок (RU корректен)

Sibling SKU 3 (КПЕЕ-100М). RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) `Котел пищеварочный Эфес КПЭЭ-100М (масляный нагрев)` корректны (`+пищеварочный` обогащение faithful). Реальных `.`-дробей нет: `7,5 (3х2,5) кВт` запятая, `24 л`/`100 л`/`700х900` целые — no-op. Напряжение `380 В` faithful (НИКОГДА). **🔶 Открытый вопрос #1 (SKU 3-8): модель-код `КПЭЭ-100М` Названия ≠ `КПЭ-100М` тела; коды не тронуты, решение Yana, НЕ блокирует.**

*(scoped к row Артикул=1145554349)*

---

## SKU 5/61 — Эфес КПЭЭ-160М котёл пищеварочный масляный нагрев 160 л (Артикул 1145576798) — без правок (RU корректен)

Sibling SKU 3/4 (КПЕЕ-160М). RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) корректны. Реальных `.`-дробей нет: `15 (6х2,5) кВт` запятая, `26 л`/`160 л`/`900х900` целые — no-op. Напряжение `380 В` faithful (НИКОГДА). **🔶 Открытый вопрос #1 (SKU 3-8): `КПЭЭ-160М` Названия ≠ `КПЭ-160М` тела; коды не тронуты, решение Yana, НЕ блокирует.**

*(scoped к row Артикул=1145576798)*

---

## SKU 6/61 — Эфес КПЭЭ-250М котёл пищеварочный масляный нагрев 250 л (Артикул 1145577701) — без правок (RU корректен)

Sibling (КПЕЕ-250М). RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) корректны. Реальных `.`-дробей нет: `15 (6х2,5) кВт` запятая, `31 л`/`250 л`/`900х1100` целые — no-op. Напряжение `380 В` faithful (НИКОГДА). Soft note: UA Назв `(масляне нагрівання)` — русизм (укр. норма `олійне`, как SKU 3-5/8); RU `(масляный нагрев)` корректен, UA faithful (не авто — locked). **🔶 Открытый вопрос #1 (SKU 3-8): `КПЭЭ-250М` Названия ≠ `КПЭ-250М` тела; коды не тронуты, решение Yana, НЕ блокирует.**

*(scoped к row Артикул=1145577701)*

---

## SKU 7/61 — Эфес КПЭЭ-400М котёл пищеварочный масляный нагрев 400 л (Артикул 1145578381) — без правок (RU корректен)

Sibling (КПЕЕ-400М). RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) корректны. Реальных `.`-дробей нет: `23 (9х2,5) кВт` запятая, `59 л`/`400 л`/`1000х1300` целые — no-op. Напряжение `380 В` faithful (НИКОГДА). Soft note: UA Назв `(масляне нагрівання)` русизм (как SKU 6), RU корректен, UA faithful. **🔶 Открытый вопрос #1 (SKU 3-8): `КПЭЭ-400М` Названия ≠ `КПЭ-400М` тела; коды не тронуты, решение Yana, НЕ блокирует.**

*(scoped к row Артикул=1145578381)*

---

## SKU 8/61 — Эфес КПЭЭ-60М котёл пищеварочный миксер масляный нагрев 60 л (Артикул 1145658361) — без правок (RU корректен)

Sibling КПЕЕ-60М с миксером (полный описательный абзац как SKU 1/2). RU самостоятельный корректный перевод (UA `…відбувається непрямо` → RU `…осуществляется косвенно`), НЕ 🔴. Назв./Назв.мод (RU) корректны. Реальных `.`-дробей нет: `0,005` МПа запятая, `21 л`/`60 л`/`45 об/мин`/`8 кВт`/`700х900` целые — no-op. Напряжение `380 В` faithful (НИКОГДА); `Потужність 8 кВт` с пробелом. UA `110 &deg;C`/`400 &deg;C` (лат. C, с пробелом) vs RU `110&deg;С`/`400&deg;С` (кир. С) — символ/энтити источника сохранены faithful. **🔶 Открытый вопрос #1 (SKU 3-8): `КПЭЭ-60М` Названия ≠ `КПЭ-60М` тела; коды не тронуты, решение Yana, НЕ блокирует.**

Наблюдение (батч-ур.): SKU 1-8 — однородный Эфес-блок котлов. SKU 1-2 КЭ-300/400 пароводяные (продолжение суб-серии chunk-012 SKU 56-61). SKU 3-8 КПЕЕ-М масляное нагревание (3-7 спец-краткие, 8 КПЕЕ-60М с миксером + полный абзац). 🔴 нет ни в одном — RU везде самостоятельный корректный перевод. Без правок все 8. Реальных `.`-дробей НЕТ (все `,` уже / целые / габариты кир. х / множители `3х2,5`/`6х2,5`/`9х2,5`). Напряжение `380 В` faithful везде (НИКОГДА). Soft notes: UA `Потужність 30кВт`/`36кВт` слитно SKU 1/2 (формат-класс C глоб.); UA Назв `(масляне нагрівання)` русизм SKU 6/7 (норма `олійне`, RU корректен, UA faithful); SKU 3 Назв (RU) без `пищеварочный` (faithful вариация источника). Новый Открытый вопрос #1 (батч-ур., SKU 3-8): модель-код Названия `КПЕЕ-NNNМ`/`КПЭЭ-NNNМ` ≠ заголовок+текст `КПЕ-NNNМ`/`КПЭ-NNNМ` (рассинхрон title↔body симметричен UA↔RU; аналог chunk-012 #1 Kogast EKTT40; коды НЕ трогаю, решение Yana, НЕ блокирует).

*(scoped к row Артикул=1145658361)*

---

## SKU 9/61 — Эфес КПЭЭ-100М котёл пищеварочный миксер масляный нагрев 100 л (Артикул 1145658954) — без правок (RU корректен)

Sibling SKU 8 (КПЕЕ-100М с миксером, краткий список характеристик). RU самостоятельный корректный перевод (UA `Котел харчоварильний…застосовується` → RU `Котел пищеварочный…применяется`), НЕ 🔴. Назв./Назв.мод (RU) `Котел пищеварочный Эфес КПЭЭ-100М (миксер, масляный нагрев)` корректны (`+пищеварочный` обогащение faithful). Реальных `.`-дробей нет: `8 кВт`/`24 л`/`100 л`/`45 об/мин`/`700х900` целые — no-op. Напряжение `380 В` faithful (НИКОГДА). `Корисний об&#39;єм`(UA)→`Полезный объем`(RU) норм Эфес-шаблон. **🔶 Открытый вопрос #1 (расширен до SKU 3-12): модель-код `КПЭЭ-100М` Названия ≠ `КПЭ-100М` заголовка+тела; рассинхрон симметричен UA↔RU; коды не тронуты, решение Yana, НЕ блокирует.**

*(scoped к row Артикул=1145658954)*

---

## SKU 10/61 — Эфес КПЭЭ-160М котёл пищеварочный миксер масляный нагрев 160 л (Артикул 1145659199) — без правок (RU корректен)

Sibling КПЕЕ-160М с миксером. RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) корректны (`+пищеварочный` faithful). Реальных `.`-дробей нет: `15,5 кВт` запятая уже, `26 л`/`160 л`/`45 об/мин`/`900х900` целые — no-op. Напряжение `380 В` faithful (НИКОГДА). **🔶 Открытый вопрос #1 (расширен до SKU 3-12): `КПЭЭ-160М` Названия ≠ `КПЭ-160М` заголовка+тела; коды не тронуты, решение Yana, НЕ блокирует.**

*(scoped к row Артикул=1145659199)*

---

## SKU 11/61 — Эфес КПЭЭ-250М котёл пищеварочный миксер масляный нагрев 250 л (Артикул 1145663178) — без правок (RU корректен)

Sibling КПЕЕ-250М с миксером. RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) корректны. Реальных `.`-дробей нет: `15,5 кВт` запятая уже, `31 л`/`250 л`/`45 об/мин`/`900х1100` целые — no-op. Напряжение `380 В` faithful (НИКОГДА). **🔶 Открытый вопрос #1 (расширен до SKU 3-12): `КПЭЭ-250М` Названия ≠ `КПЭ-250М` заголовка+тела; коды не тронуты, решение Yana, НЕ блокирует.**

*(scoped к row Артикул=1145663178)*

---

## SKU 12/61 — Эфес КПЭЭ-400М котёл пищеварочный миксер масляный нагрев 400 л (Артикул 1145664081) — без правок (RU корректен)

Sibling КПЕЕ-400М с миксером. RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) корректны. Реальных `.`-дробей нет: `23,5 кВт` запятая уже, `59 л`/`400 л`/`45 об/мин`/`1000х1300` целые — no-op. Напряжение `380 В` faithful (НИКОГДА). **🔶 Открытый вопрос #1 (расширен до SKU 3-12): `КПЭЭ-400М` Названия ≠ `КПЭ-400М` заголовка+тела; завершает суб-серию КПЕЕ-М миксер (SKU 8-12); коды не тронуты, решение Yana, НЕ блокирует.**

*(scoped к row Артикул=1145664081)*

---

## SKU 13/61 — Эфес КПЭ-60 котёл пищеварочный 60 л (Артикул 1146106219) — без правок (RU корректен)

Начало НОВОЙ суб-серии КПЕ-NN (базовые котлы без миксера/масляного обозначения). RU самостоятельный корректный перевод (UA `Котел харчоварильний…застосовується` → RU `Котел пищеварочный…применяется`), НЕ 🔴. Назв./Назв.мод (RU) `Котел пищеварочный Эфес КПЭ-60` корректны (`+пищеварочный` обогащение faithful). **Рассинхрона title↔body НЕТ:** Название `КПЕ-60`/`КПЭ-60` = заголовок+тело `КПЕ-60`/`КПЭ-60` (одиночное Е/Э, согласованно) — суб-серия КПЕ-NN НЕ затронута Открытым вопросом #1 (в отличие от КПЕЕ-М SKU 3-12). Реальных `.`-дробей нет: `10,5 (3х3,5) кВт` запятая уже, множитель `3х3,5` кир. х, `60 л`/`700х900` целые — no-op. Напряжение `380 В` faithful (НИКОГДА). UA `100 &deg;C` (лат. C, пробел) vs RU `100&deg;С` (кир. С) — символ/энтити источника сохранены faithful (паттерн SKU 8).

*(scoped к row Артикул=1146106219)*

---

## SKU 14/61 — Эфес КПЭ-100 котёл пищеварочный 100 л (Артикул 1146107615) — без правок (RU корректен)

Sibling SKU 13 (КПЕ-100). RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) `Котел пищеварочный Эфес КПЭ-100` корректны. Title↔body согласованно `КПЕ-100`/`КПЭ-100` — НЕ затронут #1. Реальных `.`-дробей нет: `15 (3х5) кВт`/`100 л`/`700х900` целые, множитель `3х5` — no-op. Напряжение `380 В` faithful (НИКОГДА). Soft note: UA `<h2>` начинается `Котел травлення Ефес КПЕ-100` — словарная ошибка источника (`травлення` = «травление/протравливание» вместо `харчоварильний`, как корректно в SKU 13/15/16); RU `<h2>Котел пищеварочный Эфес КПЭ-100` корректен. UA faithful (опечатка/ошибка источника → soft note, не авто — locked); единичная (sibling SKU 13/15/16 имеют корректное `харчоварильний`).

*(scoped к row Артикул=1146107615)*

---

## SKU 15/61 — Эфес КПЭ-160 котёл пищеварочный 160 л (Артикул 1146108664) — без правок (RU корректен)

Sibling КПЕ-160. RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) корректны. Title↔body согласованно `КПЕ-160`/`КПЭ-160` — НЕ затронут #1. UA `<h2>` корректное `Котел харчоварильний` (в отличие от SKU 14). Реальных `.`-дробей нет: `21 (6х3,5) кВт` запятая в множителе `6х3,5` уже, `160 л`/`900х900` целые — no-op. Напряжение `380 В` faithful (НИКОГДА). UA `100 &deg;C` лат./RU `100&deg;С` кир. источник faithful.

*(scoped к row Артикул=1146108664)*

---

## SKU 16/61 — Эфес КПЭ-250 котёл пищеварочный 250 л (Артикул 1146109616) — без правок (RU корректен)

Sibling КПЕ-250, завершает суб-серию КПЕ-NN батча. RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) корректны. Title↔body согласованно `КПЕ-250`/`КПЭ-250` — НЕ затронут #1. UA `<h2>` корректное `Котел харчоварильний`. Реальных `.`-дробей нет: `30 (6х5) кВт`/`250 л`/`900х1100` целые, множитель `6х5` — no-op. Напряжение `380 В` faithful (НИКОГДА). UA `100 &deg;C` лат./RU `100&deg;С` кир. источник faithful.

Наблюдение (батч-ур.): SKU 9-16 — продолжение однородного Эфес-блока котлов, ДВЕ суб-серии. SKU 9-12 = КПЕЕ-М с миксером (100/160/250/400М) — прямое продолжение SKU 8, **расширяют Открытый вопрос #1 с SKU 3-8 до SKU 3-12** (модель-код Названия `КПЕЕ`/`КПЭЭ` двойное Е/Э ≠ заголовок+тело `КПЕ`/`КПЭ` одиночное; рассинхрон title↔body симметричен UA↔RU; аналог chunk-012 #1; коды НЕ трогаю). SKU 13-16 = НОВАЯ суб-серия КПЕ-NN (базовые 60/100/160/250, без миксера/масляного обозначения) — здесь Название `КПЕ`/`КПЭ` = заголовок+тело `КПЕ`/`КПЭ` СОГЛАСОВАННО, рассинхрона НЕТ, Открытым вопросом #1 НЕ затронуты. 🔴 нет ни в одном — RU везде самостоятельный корректный перевод. Без правок все 8. Реальных `.`-дробей НЕТ (все `,` уже: `15,5`/`23,5`/`10,5`; множители `3х3,5`/`6х3,5` кир. х; остальное целые/габариты кир. х). Напряжение `380 В` faithful везде (НИКОГДА). Soft note: SKU 14 UA `<h2>Котел травлення` — словарная ошибка источника вместо `харчоварильний` (RU `пищеварочный` корректен; UA faithful, единичная — SKU 13/15/16 корректны). UA `100 &deg;C` лат. / RU `100&deg;С` кир. источник faithful везде где есть строка времени разогрева (SKU 13-16).

*(scoped к row Артикул=1146109616)*

---

## SKU 17/61 — Эфес КПЭ-400 котёл пищеварочный 400 л (Артикул 1146109981) — без правок (RU корректен)

Завершает базовую суб-серию КПЕ-NN (60/100/160/250 SKU 13-16 + 400 здесь). RU самостоятельный корректный перевод (UA `Котел харчоварильний…застосовується` → RU `Котел пищеварочный…применяется`), НЕ 🔴. Назв./Назв.мод (RU) `Котел пищеварочный Эфес КПЭ-400` корректны (`+пищеварочный` обогащение faithful). **Title↔body согласован `КПЕ-400`/`КПЭ-400` (одиночное Е/Э) — НЕ затронут Открытым вопросом #1** (как вся суб-серия КПЕ-NN). Реальных `.`-дробей нет: `40,5 кВт` запятая уже, `400 л`/`1000х1300` целые/кир. х — no-op. Напряжение `380 В` faithful (НИКОГДА). UA `100 &deg;C` (лат. C, пробел) vs RU `100&deg;С` (кир. С) — символ/энтити источника сохранены faithful.

*(scoped к row Артикул=1146109981)*

---

## SKU 18/61 — Эфес КПЭ-60 котёл пищеварочный с миксером 60 л (Артикул 1146110136) — без правок (RU корректен)

Начало НОВОЙ суб-серии КПЕ-NN **(з міксером / с миксером)** — базовый код `КПЕ`/`КПЭ` (одиночное Е/Э) + парентетический суффикс `(з міксером)`; добавлена строка `Кількість обертів міксера: 45 об/хв`. RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) `Котел пищеварочный Эфес КПЭ-60 (с миксером)` корректны (`+пищеварочный` обогащение faithful). **Title↔body согласован `КПЕ-60`/`КПЭ-60` — НЕ затронут Открытым вопросом #1** (это НЕ КПЕЕ-М двойное-Е суб-серия SKU 3-12; здесь миксер обозначен скобками, код одиночный). Реальных `.`-дробей нет: `10,5 (3х3,5) кВт` запятая уже, множитель `3х3,5` кир. х, `60 л`/`700х900` целые — no-op. Напряжение `380 В` faithful (НИКОГДА). UA `100 &deg;C` лат./RU `100&deg;С` кир. источник faithful.

*(scoped к row Артикул=1146110136)*

---

## SKU 19/61 — Эфес КПЭ-100 котёл пищеварочный с миксером 100 л (Артикул 1146110539) — без правок (RU корректен)

Sibling SKU 18 (КПЕ-100 с миксером). RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) корректны. Title↔body согласован `КПЕ-100`/`КПЭ-100` — НЕ #1. **Подтверждает единичность Открытого вопроса #2: UA `<h2>Котел харчоварильний Ефес КПЕ-100`** — здесь корректное `харчоварильний` (в отличие от SKU 14 КПЕ-100 базовый, где источник `Котел травлення`); ошибка #2 изолирована к SKU 14. Реальных `.`-дробей нет: `15 (3х5) кВт`/`100 л`/`700х900` целые, множитель `3х5` — no-op. Напряжение `380 В` faithful (НИКОГДА). UA `100 &deg;C` лат./RU `100&deg;С` кир. faithful.

*(scoped к row Артикул=1146110539)*

---

## SKU 20/61 — Эфес КПЭ-160 котёл пищеварочный с миксером 160 л (Артикул 1146111033) — без правок (RU корректен)

Sibling КПЕ-160 с миксером. RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) корректны. Title↔body согласован `КПЕ-160`/`КПЭ-160` — НЕ #1. UA `<h2>` корректное `Котел харчоварильний`. Реальных `.`-дробей нет: `21 (6х3,5) кВт` запятая в множителе `6х3,5` уже, `160 л`/`900х900` целые — no-op. Напряжение `380 В` faithful (НИКОГДА). UA `100 &deg;C` лат./RU `100&deg;С` кир. faithful.

*(scoped к row Артикул=1146111033)*

---

## SKU 21/61 — Эфес КПЭ-250 котёл пищеварочный с миксером 250 л (Артикул 1146111907) — без правок (RU корректен)

Sibling КПЕ-250 с миксером. RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) корректны. Title↔body согласован `КПЕ-250`/`КПЭ-250` — НЕ #1; UA `<h2>` содержит ` з міксером` в тексте заголовка (RU `с миксером` зеркально, faithful). Реальных `.`-дробей нет: `30 (6х5) кВт`/`250 л`/`900х1100`/`Вага 140 кг` целые, множитель `6х5` — no-op (вес `140 кг` целый — не формат-класс A `NN.00`). Напряжение `380 В` faithful (НИКОГДА). UA `100 &deg;C` лат./RU `100&deg;С` кир. faithful.

*(scoped к row Артикул=1146111907)*

---

## SKU 22/61 — Эфес КПЭ-400 котёл пищеварочный с миксером 400 л (Артикул 1146112262) — без правок (RU корректен)

Завершает суб-серию КПЕ-NN с миксером (60/100/160/250/400). RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) корректны. Title↔body согласован `КПЕ-400`/`КПЭ-400` — НЕ #1. Реальных `.`-дробей нет: `40,5 кВт` запятая уже, `400 л`/`1000х1300` целые/кир. х — no-op. Напряжение `380 В` faithful (НИКОГДА). UA `100 &deg;C` лат./RU `100&deg;С` кир. faithful.

*(scoped к row Артикул=1146112262)*

---

## SKU 23/61 — Ozti OTGD 60 котёл пищеварочный газовый (Артикул 2184924264) — RU `квт`→`кВт` (рекуррент Ozti, AUTO+откат-нота)

**Смена бренда: Эфес → Ozti** (газовые котлы прямого нагрева, новый шаблон Ozti). RU самостоятельный корректный перевод (UA `<p>Котел харчоварильнийOZTI застосовується…` → RU `<p>Котел пищеварочный OZTI применяется…`), НЕ 🔴. Назв./Назв.мод (RU) `Котел пищеварочный газовый OZTI OTGD 60` — `+пищеварочный` обогащение, валидный русский, НЕ UA-leak (паттерн SKU 1 / Ozti chunk-012 SKU 33-38 `+промышленная`/`+газовая`). **AUTO-фикс RU единицы:** `<li>Мощность, квт 11</li>` → `<li>Мощность, кВт 11</li>` — рекуррентный машинный артефакт регистра Ozti, единогласный sibling SKU 24 + кросс-чанк прецедент (chunk-010/011 SKU 75, chunk-012 SKU 7/8/33-38). UA `Потужність, квт 11` — UA faithful (UA-сторона не авто-правится, locked). Значение `11` и `Тип підключення ГАЗ` faithful (газ — напряжения нет). Габариты `800X700X850` лат. X → формат-класс B (глоб. assembly, НЕ per-SKU). Soft note: UA `харчоварильнийOZTI` слитно (пропуск пробела — артефакт UA-источника, faithful, рекуррент Ozti-шаблона).

*(scoped к row Артикул=2184924264)*

---

## SKU 24/61 — Ozti OTGI 60 котёл пищеварочный газовый 60 л (Артикул 2184928500) — RU `квт`→`кВт` (рекуррент Ozti, AUTO+откат-нота)

Sibling SKU 23 (Ozti OTGI 60, тот же газовый шаблон + строка `Об&#39;єм - 60л`/`Объем - 60л`). RU самостоятельный корректный перевод, НЕ 🔴. Назв./Назв.мод (RU) `Котел пищеварочный газовый OZTI OTGI 60` — `+пищеварочный` обогащение faithful, НЕ UA-leak. **AUTO-фикс RU единицы:** `<li>Мощность, квт 11</li>` → `<li>Мощность, кВт 11</li>` (рекуррент Ozti, единогласный sibling SKU 23 + кросс-чанк прецедент). UA `Потужність, квт 11` faithful. `Об&#39;єм`(UA)→`Объем`(RU) — норм Ozti-шаблон перевода; `60л` слитно (формат-класс C глоб., НЕ per-SKU). Габариты `800X700X850` лат. X → формат-класс B (глоб.). Значение `11`/`60л`/`ГАЗ` faithful (газ — напряжения нет). Soft note: UA `харчоварильнийOZTI` слитно (артефакт UA-источника, faithful, как SKU 23).

Наблюдение (батч-ур.): SKU 17-24 — хвост Эфес-блока + смена бренда. SKU 17 КПЭ-400 завершает базовую суб-серию КПЕ-NN (13-17: 60/100/160/250/400). SKU 18-22 — НОВАЯ суб-серия КПЕ-NN **(з міксером / с миксером)**: код `КПЕ`/`КПЭ` одиночный + парентетический `(з міксером)`, строка `Кількість обертів міксера`; title↔body согласован — **Открытым вопросом #1 НЕ затронуты** (это НЕ двойное-Е КПЕЕ-М SKU 3-12; контраст важен — миксер тут обозначен скобками, не удвоением буквы кода). SKU 23-24 — **смена бренда Эфес → Ozti** (газовые котлы OTGD/OTGI 60): RU авто-фикс единицы `Мощность, квт`→`Мощность, кВт` (рекуррентный машинный артефакт Ozti, единогласные siblings 23↔24 + кросс-чанк прецедент chunk-010/011 SKU 75 / chunk-012 SKU 7/8/33-38, откат-нота). 🔴 нет ни в одном — RU везде самостоятельный корректный перевод. Реальных `.`-дробей НЕТ (`40,5`/`10,5` кВт уже `,`; множители `3х3,5`/`6х3,5` кир. х; `15`/`21`/`30`/`140`/`11` целые; габариты Эфес кир. х / Ozti лат. X→глоб.B). Напряжение `380 В` faithful везде Эфес 17-22 (НИКОГДА); Ozti 23-24 газ — напряжения нет. Soft note: SKU 19 (КПЕ-100 с миксером) UA `<h2>` корректное `харчоварильний` — **подтверждает единичность Открытого вопроса #2** (ошибка `травлення` изолирована к SKU 14 КПЕ-100 базовый). UA `харчоварильнийOZTI` слитно SKU 23/24 (артефакт UA-источника Ozti-шаблона, faithful). Открытый вопрос #1 в батче НЕ расширяется (суб-серии КПЕ-NN базовая/с-миксером согласованы title↔body); новых вопросов нет.

*(scoped к row Артикул=2184928500)*

---
