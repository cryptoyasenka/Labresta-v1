# chunk-018 — ручная проверка для Yana (русский)

**Source:** `horoshop-export 13.05.26.xlsx` chunk-018 (37 SKU)
**Apply key:** `Артикул` (scoped per row)
**Status:** IN PROGRESS 16/37

Здесь собираю всё, что требует твоего подтверждения (не авто-фиксы). Авто-фиксы по locked-паттернам перечислены в сводках по батчам, отдельного подтверждения не требуют. Открытые вопросы накапливаются в нумерованный список и финализируются при закрытии chunk-018.

---

## Батч SKU 1-8 (8/37)

Артикулы: `1486023518` (FROSTY NES-36T печь подовая), `1909556683` (Unox XEBC06EUE1RMMP пароконв. 6 ур.), `1910258771` (Unox XEBC10EUE1RMMP пароконв. 10 ур.), `1910264280` (Unox XEBC04EUE1RMMP пароконв. 4 ур.), `1938175137` (Unox XB693MP конв. 6 ур.), `1938204361` (Unox XB893MP конв.), `1943762101` (Giorik Movair MTE7 W R пароконв.), `1980649907` (Rational iCC10-1/1E бойлерный). Смешанный батч: 1 печь подовая FROSTY + 5 Unox + 1 Giorik + 1 Rational.

### Стандартно применено (locked-паттерны, отдельного подтверждения не требуют)
- **🔴 RU=UA — 7 из 8 → автоперевод.** У SKU 1-6, 8 «Описание товара (RU)» = дословная украинская копия UA (программная проверка `desc UA==RU` True, длины совпадают). По locked-правилу → автоматический полный перевод на русский тег-в-tag 1:1. Многострочная структура (head → `<p><strong>…:</strong></p> <ul>` + перенос + строки `<li>` + перенос + `</ul> <p>…</p>`) сохранена; у SKU 1 — две группы `<ul>` (Особенности 5 `<li>` + Технические 14 `<li>`). `&ndash;`/`&mdash;`/`&reg;` и эмодзи 1:1.
- **RU корректен; правок нет — 1 из 8 (blknochg).** SKU 7 (Giorik Movair MTE7 W R): «Описание товара (RU)» — самостоятельный готовый русский перевод (≠ UA-копия, `desc UA==RU` False, len RU 1212 ≠ UA 1178) → без изменений. «Название модификации (RU)» чистый русский (0 букв іїєґ) → триплет не требуется.
- **Название модификации (RU) — украинский leak, 7 из 8 → триплет.** SKU 1-6 (украинская строка `Піч…з парозволоженням`/`…універсальна на N рівнів`), SKU 8 (форма `бойлерний`) → заменено по корректному русскому «Название (RU)» (триплет в diff). **SKU 7 — без триплета:** «Название модификации (RU)» уже чистый русский (precedent ch-016 SKU 49, ch-017 SKU 1).
- **Latin x → глобальный pass B (annotate, verbatim):** SKU 2-6 внешние размеры `860x967x843` / `860x967x1163` / `860x967x675` / `860x900x930` / `860x900x1250` — `x` латинская строчная (0x78); SKU 1 — смешанный токен `1220`**x**`1040`**х**`1700` (латинская x 0x78 + кириллическая х 0x445); SKU 7 — `907X752X837`, `X` латинская **заглавная** (0x58, targeted byte-probe — стандартный `_probe_b57x.py` класс её не ловит, подтверждено отдельным пробом U+0058). Размеры оставлены verbatim (не конвертируем в кириллицу, число не меняем), переведён только лейбл. Латинская x/X идентична UA↔RU 1:1 → рассинхрона нет → нумерованных открытых вопросов нет (precedent SKU 18/20/60/79/80).
- **Кириллическая `х` (no-op, 0x445):** SKU 1 `870х650х250` / `600х400` / `…1040х1700`; SKU 2-6 `600х400`; SKU 7 `600х400`; SKU 8 `850х842х1014` — verbatim, переведён лейбл.
- **`GN 1\1` (backslash вместо слеша, SKU 2-6):** артефакт источника, идентичен UA↔RU 1:1 → оставлен verbatim (не customer-facing рассинхрон; faithful, открытого вопроса не порождает). SKU 8 `GN 1/1` / `GN1/2` — корректный слеш.
- **`DIN 1.4301` (SKU 8):** код марки нержавеющей стали (стандарт DIN), не десятичная дробь → `1.4301` verbatim (правило `.`→`,` к кодам не применяется).
- **Значения/дроби:** реальных десятичных дробей с точкой нет — мощности `25,5` / `9,9` / `14,9` / `7,4` / `10,5` / `15,8` / `18,9 кВт` уже с запятой; `4,3 дюйма` (SKU 8) уже с запятой; `9,6 кВт` (SKU 7) уже с запятой. RU `квт`→`кВт` не требуется (везде уже `кВт`). `&deg;` сохранён как сущность; источник непоследователен по нотации `°` (SKU 4 `35 &deg;С` кириллическая С 0x421; SKU 6 `260&deg; C` пробел + латинская C) — сохранено verbatim, не нормализуем.
- **House-corpus:** снят апостроф-entity при переводе украинского слова в русское — SKU 1 `під'єднання`→`подсоединение`; SKU 2 `пам'яті`→`памяти`, `м'ясо`→`мясо`; SKU 3/4 `пам'яті`→`памяти`; SKU 5 `м'ясних`→`мясных`, `м'яса`→`мяса`; SKU 6 `м'ясних`→`мясных`, `об'єм`→`объём`, `Зв'яжіться`→`Свяжитесь`.
- **Voltage:** значения не тронуты (SKU 1/2/3/5/6/7/8 `380 В`; SKU 4 `220/380 В`); переведён только лейбл `Підключення до електромережі:` / `напруга` → `Подключение к электросети:` / `напряжение`.

### Заслуживает внимания (soft, без блокировки)
- **SKU 5/6 (Unox XB693MP / XB893MP, арт. 1938175137 / 1938204361):** в украинском `Робоча температура: від 30 до 260&deg;C` один `&deg;C` (при 260, не при 30). В русском (🔴 авто-перевод) — faithful `Рабочая температура: от 30 до 260&deg;C` (правило нормализации температурного диапазона применяется к genuine-RU FLAG, не к автоматической 🔴 копии). Решения не требует.
- **SKU 7 (Giorik Movair MTE7 W R, арт. 1943762101):** в украинском источнике `<li>Температура температури: до 280&deg;C.</li>` — дефект источника (вместо «Робоча температура»). Самостоятельный готовый русский перевод зеркалит дефект: `Температура температуры: до 280&deg;C.`. Число `280` совпадает UA↔RU, не customer-facing рассинхрон. blknochg — корректное RU не переписываем. Faithful, решения не требует.
- **META keywords (SKU 1-8):** оставлены faithful со всеми украинскими/leak-артефактами и опечатками источника (`Frosty×`, `unox78893`, `печінка`, `жіорик`/`жорок`, `перука`/`парик`, обрезанные хвосты). META по правилу всегда faithful.

### Сводка батча

| SKU | Артикул | 🔴 RU=UA / blknochg | Назв.мод триплет | Прочее |
|---|---|---|---|---|
| 1 | 1486023518 | 🔴 FROSTY NES-36T (2×`<ul>`) | ✅ | смеш. `1220x1040х1700` (лат.0x78+кир.0x445)→глоб.B/no-op; кир.х `870х650х250`/`600х400`; `під'єднання`→подсоединение; `25,5 кВт`; `380 В` |
| 2 | 1909556683 | 🔴 Unox XEBC06EUE1RMMP | ✅ | latin-x `860x967x843`→глоб.B; кир.х `600х400`; `GN 1\1` арт.verbatim; `пам'яті`→памяти, `м'ясо`→мясо; `9,9 кВт`; `380 В` |
| 3 | 1910258771 | 🔴 Unox XEBC10EUE1RMMP | ✅ | latin-x `860x967x1163`→глоб.B; кир.х `600х400`; `GN 1\1` verbatim; `пам'яті`→памяти; `14,9 кВт`; `380 В` |
| 4 | 1910264280 | 🔴 Unox XEBC04EUE1RMMP | ✅ | latin-x `860x967x675`→глоб.B; кир.х `600х400`×2; `GN 1\1` verbatim; `35 &deg;С` кир.С verbatim; `7,4 кВт`; `220/380 В` |
| 5 | 1938175137 | 🔴 Unox XB693MP | ✅ | latin-x `860x900x930`→глоб.B; кир.х `600х400`; `GN 1\1` verbatim; `м'ясних`→мясных; soft `от 30 до 260&deg;C`; `10,5 кВт`; `380 В` |
| 6 | 1938204361 | 🔴 Unox XB893MP | ✅ | latin-x `860x900x1250`→глоб.B; кир.х `600х400`; `GN 1\1` verbatim; `260&deg; C` пробел+лат.C verbatim; `об'єм`→объём; `15,8 кВт`; `380 В` |
| 7 | 1943762101 | blknochg (RU genuine) | — (чистый рус.) | описание без изменений; **`907X752X837` ЗАГЛАВНАЯ латинская X 0x58**→глоб.B verbatim; кир.х `600х400`; soft `Температура температури`→`температуры`; `9,6 кВт`; `380 В` |
| 8 | 1980649907 | 🔴 Rational iCC10-1/1E | ✅ (`бойлерний`→`бойлерный`) | кир.х `850х842х1014`; `GN 1/1`/`GN1/2` слеш; `DIN 1.4301` код verbatim; `4,3 дюйма`; `&reg;`; `18,9 кВт`; `380 В` |

Открытых вопросов по батчу: **0** (правки — locked-паттерны 🔴/триплет/глоб.B/House; blknochg SKU 7 корректен без изменений; soft-наблюдения SKU 5/6/7 только информируют, решения не требуют).

---

## Батч SKU 9-16 (16/37)

Артикулы: `1980775973` (Rational CMP61 бойлерный), `1994512017` (Apach Squero AP5QT), `2037407063` (Frosty ESD-8A конв.), `2043133894` (Frosty EN-50 конв.), `2053886588` (Tecnoeka EKF423UD/0/149 пароконв.), `2062021637` (TATRA TPI 10 M.V пароконв.), `2115408487` (Apach AV043D конв. с расстойкой), `2115428300` (Apach AV04DC конв. с расстойкой). Смешанный батч: Rational + Apach×3 + Frosty×2 + Tecnoeka + TATRA.

### Стандартно применено (locked-паттерны, отдельного подтверждения не требуют)
- **🔴 RU=UA — 7 из 8 → автоперевод.** У SKU 9, 10, 11, 12, 14, 15, 16 «Описание товара (RU)» = дословная украинская копия UA (программная проверка `desc UA==RU` True, длины совпадают: 2394/2142/2303/2321/2602/1309/2724). По locked-правилу → автоматический полный перевод на русский тег-в-tag 1:1. Многострочная структура (head → `<ul>` + строки `<li>` + `</ul> <p>…</p>`; SKU 11/12/16 — `<h3>`/`<h4>` и `<br />` внутри `<p>` сохранены зеркально; SKU 11 — 4 группы `<ul>`). `&ndash;`/`&mdash;`/`&deg;` и эмодзи 1:1.
- **RU корректен; правок нет — 1 из 8 (blknochg).** SKU 13 (Tecnoeka EKF423UD/0/149): «Описание товара (RU)» — самостоятельный готовый русский перевод (≠ UA-копия, `desc UA==RU` False, len RU 2672 ≠ UA 2607) → без изменений. «Название модификации (RU)» чистый русский, совпадает с «Название (RU)» → триплет не требуется.
- **Название модификации (RU) — украинский leak, 5 из 8 → триплет.** SKU 9 (`…бойлерний` форма с укр. окончанием, как ch-018 SKU 8), SKU 11/12 (`Піч конвекційна…`), SKU 15/16 (`Піч конвекційна Apach AV043D/AV04DC` — укр. + усечённая форма) → заменено по корректному русскому «Название (RU)» (триплет в diff). **SKU 10/14 — без триплета (blknotrip):** «Название модификации (RU)» уже чистый русский бренд+код (`Пароконвектомат Apach Squero AP5QT` / `Пароконвектомат TATRA TPI 10 M.V`), 0 букв іїєґ (precedent ch-016 SKU 49, ch-017 SKU 1).
- **Latin x → глобальный pass B (annotate, verbatim):** SKU 9 `847x771x782` (`x` 0x78); SKU 12 `840мм x 675мм x 580мм` (`x` 0x78 между значениями) + `(Д*Ш*В)` (`*` 0x2A); SKU 14 — `x` 0x78 **в лейбле** `ШxГxВ` и между значениями (`817 x 888 x 1308`, `817 x 825 x 1308`, `660 x 490 x 948`, `895 x 970 x 1460`). Размеры/лейблы оставлены verbatim (латинскую x НЕ конвертируем в кириллицу, число не меняем), переведён только текст лейбла. Латинская x идентична UA↔RU 1:1 → рассинхрона нет → нумерованных открытых вопросов нет (precedent SKU 18/20/60/79/80).
- **Кириллическая `х` (no-op, 0x445):** SKU 10 `937х821х715` / `GN 1/1 600х400`; SKU 11 `600х400х30`; SKU 12 `600х400х20`; SKU 14 `GN 1/1 600 х 400`; SKU 15 `GN2/3+460х340` / `600х770х540`; SKU 16 `GN1/1+600х400` / `800х840х541` — verbatim, переведён лейбл.
- **Реальная `.`-дробь → запятая (обе локали):** SKU 15/16 `2.4 дюйми` → `2,4 дюйма` (диагональ дисплея, точка U+002E — реальная десятичная дробь; locked-правило `.`→`,` + `дюйми`→`дюйма`). **`DIN 1.4301` (SKU 9):** код марки нержавеющей стали (стандарт DIN), не десятичная дробь → `1.4301` verbatim. Прочие значения с запятой уже (`10,5`/`6,40`/`15,8`/`3,4`/`6,3 кВт`, `25,2А`, `22,4 л/ч`). RU `квт`→`кВт` не требуется.
- **`&deg;` / нотация:** источник непоследователен — SKU 11 `+50 &deg; С` (пробел + кириллическая С 0x421), SKU 12 `+50&deg;С` (кир. С) и `(50&deg;C&ndash;300&deg;C)` (лат. C), SKU 15/16 `260&deg;С` (кир. С), SKU 16 `270&deg;C` (лат. C) — `&deg;` сущность и С/C сохранены verbatim (faithful mirror, 🔴 авто-копия, нормализацию температурного диапазона к 🔴 не применяем). SKU 14 `5&deg;f` (жёсткость воды) — сущность сохранена.
- **`&rdquo;` (SKU 14 `3/4&rdquo; M`):** правая двойная кавычка / обозначение дюйма — НЕ апостроф → сохранена verbatim как сущность (в отличие от `&#39;`/`&rsquo;`, которые снимаются при переводе укр. слова).
- **House-corpus:** снят апостроф-entity при переводе украинского слова в русское — SKU 9 `під'єднання`→`подсоединение`, `м'ясних`→`мясных`; SKU 10 `під'єднання`→`подсоединение`; SKU 11 `м'яса`→`мяса`; SKU 12 `м'ясних`→`мясных`; SKU 16 `м'ясних`→`мясных`. House: `Гастроємність`→`Гастроемкость` (SKU 14 лейбл `Тип гастроемкостей:`).
- **Voltage:** значения не тронуты (SKU 9/10 `380 В`; SKU 12 `220В`; SKU 14 `220 / 380-415 В 3N ~`; SKU 15 `220 В`; SKU 16 `220/380В`); переведён только лейбл (`Підключення до електромережі:`/`Підключення до мережі:` → `Подсоединение к электросети:`/`Подключение к сети:`).

### Заслуживает внимания (soft, без блокировки)
- **SKU 13 (Tecnoeka EKF423UD/0/149, арт. 2053886588):** в genuine-RU заголовке `…профессиональной кухни 🍴.</h3>` лишняя точка после эмодзи (UA — `кухні 🍴</h3>` без точки). Артефакт пунктуации готового русского текста, не customer-facing рассинхрон чисел/кодов. blknochg — корректное RU не переписываем. Faithful, решения не требует.
- **META keywords (SKU 9-16):** оставлены faithful со всеми украинскими/leak-артефактами и опечатками источника (`апачь`, `параконвектом`, `пароконвестомати`, `Парокостома`, обрезанные хвосты). META по правилу всегда faithful.

### Сводка батча

| SKU | Артикул | 🔴 RU=UA / blknochg | Назв.мод триплет | Прочее |
|---|---|---|---|---|
| 9 | 1980775973 | 🔴 Rational CMP61 | ✅ (`бойлерний`→`бойлерный`) | latin-x `847x771x782`→глоб.B; `DIN 1.4301` код verbatim; `під'єднання`→подсоединение, `м'ясних`→мясных; `11 кВт`; `380 В` |
| 10 | 1994512017 | 🔴 Apach Squero AP5QT | — (чистый рус. бренд+код) | blknotrip; кир.х `937х821х715`/`600х400` no-op; `под'єднання`→подсоединение; `10,5 кВт`; `380 В` |
| 11 | 2037407063 | 🔴 Frosty ESD-8A | ✅ (`Піч`→`Печь`) | `<h3>`+эмодзи+`<br />` 1:1, 4×`<ul>`; `+50 &deg; С` кир.С verbatim; кир.х `600х400х30`; `м'яса`→мяса; напряжения нет |
| 12 | 2043133894 | 🔴 Frosty EN-50 | ✅ (`Піч`→`Печь`) | `(50&deg;C&ndash;300&deg;C)` лат.C; `840мм x 675мм` лат.x→глоб.B + `(Д*Ш*В)` `*` verbatim; кир.х `600х400х20`; `6,40 кВт/220В` voltage не тронут; `м'ясних`→мясных |
| 13 | 2053886588 | blknochg (RU genuine) | — (чистый рус.) | описание без изменений; soft RU title `🍴.` лишняя точка vs UA — faithful; len RU 2672≠UA 2607 |
| 14 | 2062021637 | 🔴 TATRA TPI 10 M.V | — (чистый рус. бренд+код) | blknotrip; **latin-x 0x78 в лейбле `ШxГxВ` + значениях `817 x 888 x 1308`…→глоб.B verbatim**; кир.х `600 х 400`; `&rdquo;` verbatim; `15,8 кВт`; voltage `220/380-415 В` не тронут |
| 15 | 2115408487 | 🔴 Apach AV043D | ✅ (→`Печь…с расстойкой`) | **`2.4 дюйми`→`2,4 дюйма`** (реальная дробь); кир.х `460х340`/`600х770х540`; `260&deg;С` кир.С; `3,4 кВт, 220 В` не тронут |
| 16 | 2115428300 | 🔴 Apach AV04DC | ✅ (→`Печь…с расстойкой`) | **`2.4 дюйми`→`2,4 дюйма`**; кир.х `600х400`/`800х840х541`; `270&deg;C` лат.C/`260&deg;С` кир.С; `6,3 кВт, 220/380В` не тронут; `м'ясних`→мясных |

Открытых вопросов по батчу: **0** (правки — locked-паттерны 🔴/триплет/глоб.B/House/реальная `.`-дробь; blknochg SKU 13 корректен без изменений; soft-наблюдение SKU 13 только информирует, решения не требует).

---

## Открытые вопросы chunk-018

_(пока пусто — наполняется при аудите батчей)_

---

**Last updated:** 2026-05-18 — chunk-018 батч SKU 9-16 (16/37): смешанный — 🔴 RU=UA 7/8 (SKU9 Rational CMP61 + SKU10 Apach Squero AP5QT + SKU11/12 Frosty ESD-8A/EN-50 + SKU14 TATRA TPI 10 M.V + SKU15/16 Apach AV043D/AV04DC — укр.копия→AUTO перевод тег-в-tag, прогр. `desc UA==RU` True, длины совпадают) + blknochg 1/8 (SKU13 Tecnoeka EKF423UD/0/149 — genuine RU, len 2672≠2607). Назв.мод-leak→триплет 5/8 (SKU9 `бойлерний`→`бойлерный`; SKU11/12 `Піч`→`Печь`; SKU15/16 →`Печь…с расстойкой`); SKU10/14 blknotrip (Назв.мод чистый бренд+код); SKU13 без триплета (RU корректен). Latin-x 0x78 глоб.B: SKU9 `847x771x782`, SKU12 `840мм x 675мм` + `(Д*Ш*В)` `*` 0x2A, SKU14 в лейбле `ШxГxВ` + значениях `817 x 888 x 1308`… (verbatim, UA↔RU 1:1, нумерованного вопроса нет). Кир.х 0x445 SKU10/11/12/14/15/16 no-op. Реальная `.`-дробь SKU15/16 `2.4 дюйми`→`2,4 дюйма` (locked); `DIN 1.4301` (SKU9) код стали verbatim; прочие `10,5`/`6,40`/`15,8`/`3,4`/`6,3 кВт` запятая уже. `&deg;` нотация непоследовательна (кир.С 0x421 / лат.C) verbatim (faithful 🔴). `&rdquo;` (SKU14 `3/4&rdquo;`) НЕ апостроф — verbatim. House: SKU9/10 `під'єднання`→подсоединение, SKU9 `м'ясних`, SKU11 `м'яса`, SKU12/16 `м'ясних` (снят апостроф-entity), SKU14 Гастроємність→Гастроемкость. Voltage SKU9/10 `380 В`, SKU12 `220В`, SKU14 `220/380-415 В`, SKU15 `220 В`, SKU16 `220/380В` не тронуты (лейбл переведён). soft SKU13 genuine-RU title `🍴.` лишняя точка vs UA — faithful, не нумеровано. META always faithful. Открытых вопросов chunk-018 = 0. NEXT: батч SKU 17-24.
