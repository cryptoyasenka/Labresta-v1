---
phase: 04-management-ui-and-authentication
plan: 06
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-03
files_modified:
  - app/views/logs.py
  - app/views/settings.py
  - app/templates/logs/index.html
  - app/templates/settings/users.html
  - app/templates/settings/sync.html
autonomous: true
requirements:
  - AUTH-02
  - DASH-04

must_haves:
  truths:
    - "Admin can browse detailed sync logs with filters by date, supplier, and status"
    - "Admin can manage users: create, edit, deactivate, change password, assign role"
    - "Admin can see user activity: last login and processed match count"
    - "Operator role cannot access user management"
    - "Sync settings (interval, thresholds) viewable on settings page"
  artifacts:
    - path: "app/views/logs.py"
      provides: "Logs blueprint with filtered log viewer"
      exports: ["logs_bp"]
    - path: "app/views/settings.py"
      provides: "Settings blueprint with user management and sync config"
      exports: ["settings_bp"]
    - path: "app/templates/logs/index.html"
      provides: "Log viewer page"
    - path: "app/templates/settings/users.html"
      provides: "User management page (admin only)"
    - path: "app/templates/settings/sync.html"
      provides: "Sync settings page"
  key_links:
    - from: "app/views/settings.py"
      to: "app/models/user.py"
      via: "User CRUD operations"
      pattern: "User"
    - from: "app/views/logs.py"
      to: "app/models/sync_run.py"
      via: "SyncRun queries with detailed filtering"
      pattern: "SyncRun"
---

<objective>
Build the logs section for detailed sync history viewing and the settings section for user management and sync configuration.

Purpose: DASH-04 requires a sync journal with detail beyond the dashboard summary. AUTH-02 requires multiple users with roles, and the user requested full CRUD for user management. These admin tools complete the management panel.
Output: Detailed log viewer with filters, user management CRUD (admin only), sync settings page.
</objective>

<execution_context>
@C:/Users/Yana/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Yana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-management-ui-and-authentication/04-RESEARCH.md
@.planning/phases/04-management-ui-and-authentication/04-01-SUMMARY.md
@.planning/phases/04-management-ui-and-authentication/04-03-SUMMARY.md

<interfaces>
From app/models/user.py (created in Plan 01):
```python
class User(db.Model, UserMixin):
    id, email, password_hash, name, role (admin|operator),
    is_active, last_login_at, matches_processed, created_at
    set_password(password), check_password(password)
    @property is_admin
```

From app/models/sync_run.py:
```python
class SyncRun(db.Model):
    id, supplier_id, started_at, completed_at, status,
    products_fetched, products_created, products_updated,
    products_disappeared, match_candidates_generated, error_message
    supplier = relationship("Supplier")
```

Admin required decorator pattern (from RESEARCH.md):
```python
def admin_required(f):
    @wraps(f)
    def decorated_function(*args, **kwargs):
        if not current_user.is_authenticated or not current_user.is_admin:
            abort(403)
        return f(*args, **kwargs)
    return decorated_function
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create logs blueprint with detailed sync log viewer and filtering</name>
  <files>
    app/views/logs.py
    app/templates/logs/index.html
  </files>
  <action>
    1. Create `app/views/logs.py` with `logs_bp = Blueprint("logs", __name__)`:

       **GET /logs/** — Detailed sync log viewer:
       - Query SyncRun with eager load of supplier relationship
       - Filter params:
         - `supplier_id` (dropdown)
         - `status` (success|error|running|all, default=all)
         - `date_from` (date input, format YYYY-MM-DD)
         - `date_to` (date input)
         - `page`, `per_page` (25|50|100)
       - Sort by started_at DESC by default
       - For each sync run, show full detail:
         - Supplier name
         - Started at, Completed at (formatted with timezone)
         - Duration (completed_at - started_at, formatted)
         - Status (colored badge)
         - Products: fetched, created, updated, disappeared
         - Match candidates generated
         - Error message (full text, expandable if long)
       - Paginate results

       **GET /logs/<int:sync_run_id>** — Single sync run detail (optional expansion):
       - Full detail page for a single sync run
       - All fields displayed, error message fully visible

    2. Register in `app/__init__.py`:
       ```python
       from app.views.logs import logs_bp
       app.register_blueprint(logs_bp, url_prefix="/logs")
       ```

    3. Create `app/templates/logs/index.html` extending base.html:
       - Page title: "Журнал синхронизаций"
       - Filter bar: supplier dropdown, status dropdown, date range inputs, per-page selector
       - Table with all sync run details:
         - Поставщик, Начало, Длительность, Статус (badge), Получено, Создано, Обновлено, Исчезло, Матчи, Ошибка (truncated, expandable)
       - Each row expandable (click to see full error message if present)
       - Pagination controls
       - Show total count and summary stats: "Всего синков: N, Успешных: M, Ошибок: K"
       - Empty state: "Синхронизации ещё не выполнялись"

    4. Update base.html nav: replace placeholder Logs link with `{{ url_for('logs.index') }}`
  </action>
  <verify>
    <automated>cd C:/Projects/labresta-sync && uv run python -c "
from app import create_app
app = create_app()
with app.test_client() as c:
    r = c.get('/logs/')
    assert r.status_code == 302, f'Logs should require auth: {r.status_code}'
    print('Logs page requires auth')
    print('All checks passed')
"</automated>
  </verify>
  <done>Logs page shows detailed sync history with filtering by supplier/status/date range, pagination, expandable error messages</done>
</task>

<task type="auto">
  <name>Task 2: Create settings blueprint with user management CRUD and sync settings</name>
  <files>
    app/views/settings.py
    app/templates/settings/users.html
    app/templates/settings/sync.html
  </files>
  <action>
    1. Create `app/views/settings.py` with `settings_bp = Blueprint("settings", __name__)`:

       Implement `admin_required` decorator (from RESEARCH.md pattern):
       ```python
       from functools import wraps
       from flask import abort
       from flask_login import current_user

       def admin_required(f):
           @wraps(f)
           def decorated_function(*args, **kwargs):
               if not current_user.is_authenticated or not current_user.is_admin:
                   abort(403)
               return f(*args, **kwargs)
           return decorated_function
       ```

       **GET /settings/users** — User list (admin only):
       - Decorated with `@login_required` and `@admin_required`
       - Query all User records
       - Display: name, email, role (badge), is_active (badge), last_login_at, matches_processed, created_at

       **POST /settings/users/create** — Create new user (admin only):
       - JSON or form body: email, name, password, role (admin|operator)
       - Validate: email unique, password min length 8
       - Create User, set_password
       - Flash success, redirect to user list

       **POST /settings/users/<int:user_id>/edit** — Edit user (admin only):
       - Update name, email, role
       - Prevent admin from demoting themselves if they're the last admin
       - Flash success, redirect

       **POST /settings/users/<int:user_id>/password** — Change password (admin only):
       - New password min length 8
       - Call user.set_password(new_password)

       **POST /settings/users/<int:user_id>/toggle** — Toggle active/inactive (admin only):
       - Flip user.is_active
       - Prevent deactivating self
       - Flash result

       **GET /settings/sync** — Sync settings view (admin only):
       - Show current APScheduler configuration:
         - Sync interval (from scheduler job)
         - Matching thresholds (from environment or config)
         - Last sync time per supplier
       - Information display only for MVP (editing scheduler interval requires app restart, so display-only with note)

    2. Register in `app/__init__.py`:
       ```python
       from app.views.settings import settings_bp
       app.register_blueprint(settings_bp, url_prefix="/settings")
       ```

    3. Create `app/templates/settings/users.html` extending base.html:
       - Page title: "Управление пользователями"
       - "Создать пользователя" button (opens modal or inline form)
       - User creation form: email, name, password (with confirmation), role dropdown (admin/operator)
       - User table: Имя, Email, Роль (badge: admin=primary, operator=secondary), Активен (badge: green/red), Последний вход, Обработано матчей, Создан, Действия
       - Actions column: "Изменить" (modal or inline), "Сменить пароль" (modal), "Активировать/Деактивировать" toggle button
       - User activity: last_login_at formatted as relative time, matches_processed as number
       - Admin-only protection: page returns 403 for operator role

    4. Create `app/templates/settings/sync.html` extending base.html:
       - Page title: "Настройки синхронизации"
       - Display card: current sync interval (4 hours), misfire grace time (15 min)
       - Display card: matching thresholds — CONFIDENCE_HIGH (80), CONFIDENCE_MEDIUM (60), cutoff (60), MAX_PRICE_RATIO (3.0)
       - Per-supplier status table: supplier name, last fetched, status, feed URL
       - Info note: "Для изменения параметров отредактируйте конфигурацию и перезапустите приложение"
       - Link from dashboard settings area (collapsible panel)

    5. Update base.html nav: add "Настройки" dropdown (admin only — conditionally shown):
       - "Пользователи" → `{{ url_for('settings.user_list') }}`
       - "Синхронизация" → `{{ url_for('settings.sync_settings') }}`
       - Show only when `current_user.is_admin`
  </action>
  <verify>
    <automated>cd C:/Projects/labresta-sync && uv run python -c "
from app import create_app
app = create_app()
with app.test_client() as c:
    # Settings should require auth
    r = c.get('/settings/users')
    assert r.status_code == 302, f'Users page should require auth: {r.status_code}'
    print('Settings pages require auth')
    r = c.get('/settings/sync')
    assert r.status_code == 302, f'Sync settings should require auth: {r.status_code}'
    print('Sync settings require auth')
    print('All checks passed')
"</automated>
  </verify>
  <done>User management CRUD works for admins (create, edit, deactivate, change password, assign role), operator role blocked, sync settings display current configuration</done>
</task>

</tasks>

<verification>
1. Navigate to /logs/ — sync history displayed with all details
2. Filter logs by supplier, status, date range — correct results
3. Expand a log entry with error — full error message visible
4. Navigate to /settings/users as admin — user list displayed
5. Create new user with operator role — user appears in list
6. Edit user's name — change reflected
7. Change user's password — old password no longer works
8. Deactivate user — user marked inactive, can't log in
9. As operator: navigate to /settings/users — 403 Forbidden
10. /settings/sync shows current configuration values
</verification>

<success_criteria>
- Detailed sync log viewer with date/supplier/status filters and pagination
- User CRUD: create, edit, deactivate, change password, assign role
- Admin-only access to user management (403 for operators)
- User activity visible: last login, matches processed
- Sync settings display (read-only for MVP)
- Navigation shows Settings dropdown only for admins
</success_criteria>

<output>
After completion, create `.planning/phases/04-management-ui-and-authentication/04-06-SUMMARY.md`
</output>
