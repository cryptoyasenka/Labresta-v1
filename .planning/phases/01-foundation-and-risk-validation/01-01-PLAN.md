---
phase: 01-foundation-and-risk-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - app/__init__.py
  - app/config.py
  - app/extensions.py
  - app/models/__init__.py
  - app/models/supplier.py
  - app/models/catalog.py
  - app/models/supplier_product.py
  - app/views/__init__.py
  - app/views/main.py
  - app/views/suppliers.py
  - app/templates/base.html
  - app/templates/index.html
  - app/templates/suppliers/list.html
  - app/templates/suppliers/form.html
  - .env.example
  - .gitignore
  - run.py
autonomous: true
requirements: [SUPP-01, SUPP-02]

must_haves:
  truths:
    - "Flask app starts on localhost:5000 without errors"
    - "Admin can add a new supplier with name, feed URL, and discount %"
    - "Admin can edit an existing supplier's name, URL, and discount"
    - "Admin can enable/disable a supplier via toggle"
    - "Supplier list page shows all suppliers with their status"
  artifacts:
    - path: "app/__init__.py"
      provides: "Flask app factory with blueprint registration"
      contains: "create_app"
    - path: "app/models/supplier.py"
      provides: "Supplier ORM model"
      contains: "class Supplier"
    - path: "app/models/catalog.py"
      provides: "PromProduct ORM model"
      contains: "class PromProduct"
    - path: "app/models/supplier_product.py"
      provides: "SupplierProduct ORM model"
      contains: "class SupplierProduct"
    - path: "app/views/suppliers.py"
      provides: "Supplier CRUD blueprint"
      contains: "suppliers_bp"
    - path: "app/templates/suppliers/form.html"
      provides: "Add/edit supplier form"
      min_lines: 20
  key_links:
    - from: "app/views/suppliers.py"
      to: "app/models/supplier.py"
      via: "SQLAlchemy queries"
      pattern: "Supplier"
    - from: "app/__init__.py"
      to: "app/views/suppliers.py"
      via: "blueprint registration"
      pattern: "register_blueprint.*suppliers"
---

<objective>
Scaffold the entire Flask project and deliver working supplier CRUD through a minimal web UI.

Purpose: Establish the foundation that all subsequent plans build on -- app factory, database models, and the first working web pages. The user gets a running app on day one.
Output: Running Flask app at localhost:5000 with supplier add/edit/enable/disable via Bootstrap forms.
</objective>

<execution_context>
@C:/Users/Yana/.claude-acc1/get-shit-done/workflows/execute-plan.md
@C:/Users/Yana/.claude-acc1/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-risk-validation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project skeleton with Flask app factory, config, and all DB models</name>
  <files>
    pyproject.toml
    app/__init__.py
    app/config.py
    app/extensions.py
    app/models/__init__.py
    app/models/supplier.py
    app/models/catalog.py
    app/models/supplier_product.py
    app/views/__init__.py
    app/views/main.py
    .env.example
    .gitignore
    run.py
  </files>
  <action>
    1. Create `pyproject.toml` with project metadata and dependencies:
       - Runtime: flask, flask-sqlalchemy, sqlalchemy, alembic, lxml, requests, python-dotenv, openpyxl, chardet
       - Dev: pytest, pytest-flask, ruff
       - Use uv as package manager. Set `requires-python = ">=3.11"`.

    2. Create `app/extensions.py` with shared extension instances:
       ```python
       db = SQLAlchemy()
       ```

    3. Create `app/config.py` with config classes:
       - `DefaultConfig`: loads from .env via python-dotenv. SQLALCHEMY_DATABASE_URI using `os.path.join(basedir, 'instance', 'labresta.db')` with `sqlite:///` prefix. SECRET_KEY from env. FTP_HOST, FTP_USER, FTP_PASS, FTP_REMOTE_PATH from env (all optional, used in Plan 04).
       - SQLALCHEMY_TRACK_MODIFICATIONS = False.

    4. Create `app/__init__.py` with `create_app(config_name='default')`:
       - `os.makedirs(app.instance_path, exist_ok=True)` for Windows safety (see Research Pitfall 4).
       - Load config from `app.config.DefaultConfig`.
       - Init db with app.
       - Register blueprints: main_bp (no prefix), suppliers_bp (/suppliers), catalog_bp (/catalog -- placeholder, implemented in Plan 03).
       - `db.create_all()` inside app context for first run.

    5. Create all three SQLAlchemy models exactly as specified in 01-RESEARCH.md "SQLite Schema" section:
       - `Supplier` in `app/models/supplier.py` (id, name, feed_url, discount_percent, is_enabled, created_at, updated_at, last_fetched_at, last_fetch_status, last_fetch_error)
       - `PromProduct` in `app/models/catalog.py` (id, external_id unique, name, brand, model, article, price as integer cents, currency, imported_at, indexes on brand and name)
       - `SupplierProduct` in `app/models/supplier_product.py` (id, supplier_id FK, external_id, name, brand, model, article, price_cents, currency, available, last_seen_at, unique constraint on supplier_id+external_id)
       - Use `datetime.now(timezone.utc)` for all timestamps (not naive datetimes).
       - Import all models in `app/models/__init__.py` so they register with SQLAlchemy.

    6. Create `app/views/main.py` with `main_bp` blueprint:
       - Index route `/` renders `index.html` with link to suppliers list.

    7. Create `.env.example` with all config vars (SECRET_KEY, FTP_HOST, FTP_USER, FTP_PASS, FTP_REMOTE_PATH) and comments. No actual secrets.

    8. Update `.gitignore`: add `instance/`, `.env`, `__pycache__/`, `*.pyc`, `.ruff_cache/`, `dist/`, `*.egg-info/`.

    9. Create `run.py`:
       ```python
       from app import create_app
       app = create_app()
       if __name__ == '__main__':
           app.run(debug=True)
       ```

    10. Run `uv sync` to install all dependencies.
  </action>
  <verify>
    <automated>cd C:/Projects/labresta-sync && uv run python -c "from app import create_app; app = create_app(); print('OK:', list(app.url_map.iter_rules()))"</automated>
  </verify>
  <done>Flask app creates without errors, all 3 models register with SQLAlchemy, URL routes include / and /suppliers/* paths, instance/labresta.db is created on first run.</done>
</task>

<task type="auto">
  <name>Task 2: Build supplier CRUD views and Bootstrap templates</name>
  <files>
    app/views/suppliers.py
    app/templates/base.html
    app/templates/index.html
    app/templates/suppliers/list.html
    app/templates/suppliers/form.html
  </files>
  <action>
    1. Create `app/templates/base.html`:
       - Bootstrap 5.3 via CDN (link tag + script tag). No local CSS files.
       - Navbar with app name "LabResta Sync" and links: Home, Suppliers, Import Catalog (placeholder href for Plan 03).
       - `{% block content %}{% endblock %}` for page content.
       - Flash message rendering at top of content block (for success/error messages).

    2. Create `app/templates/index.html`:
       - Extends base.html. Welcome message. Links to Suppliers list and Import Catalog.

    3. Create `app/templates/suppliers/list.html`:
       - Extends base.html. Table with columns: Name, Feed URL (truncated to 50 chars), Discount %, Status (enabled/disabled badge), Actions (Edit, Toggle enable/disable).
       - "Add Supplier" button linking to the add form.
       - If no suppliers exist, show a friendly "No suppliers yet" message with add button.

    4. Create `app/templates/suppliers/form.html`:
       - Extends base.html. Form with fields: Name (text, required), Feed URL (url, required), Discount % (number, step=0.01, min=0, max=100, default 0).
       - Submit button text: "Add Supplier" or "Update Supplier" depending on context.
       - Cancel button linking back to list.
       - Server-side validation errors displayed next to fields.

    5. Create `app/views/suppliers.py` with `suppliers_bp` blueprint (url_prefix='/suppliers'):
       - `GET /suppliers/` — List all suppliers ordered by name. Render list.html.
       - `GET /suppliers/add` — Show empty form. Render form.html.
       - `POST /suppliers/add` — Validate (name required, feed_url required, discount 0-100). Create Supplier. Flash success. Redirect to list.
       - `GET /suppliers/<id>/edit` — Show pre-filled form. Render form.html with supplier data.
       - `POST /suppliers/<id>/edit` — Validate and update. Flash success. Redirect to list.
       - `POST /suppliers/<id>/toggle` — Toggle is_enabled. Flash status change. Redirect to list.
       - Use SQLAlchemy 2.0 style: `db.session.execute(select(Supplier))` not `Supplier.query`.
       - All POST routes redirect after success (PRG pattern).

    6. Create placeholder `app/views/catalog.py` with empty `catalog_bp` blueprint (url_prefix='/catalog') and a single index route that renders a "Coming soon" page. This prevents import errors in create_app(). Full implementation in Plan 03.
  </action>
  <verify>
    <automated>cd C:/Projects/labresta-sync && uv run python -c "
from app import create_app
app = create_app()
client = app.test_client()
# Test index
r = client.get('/')
assert r.status_code == 200, f'Index failed: {r.status_code}'
# Test supplier list (empty)
r = client.get('/suppliers/')
assert r.status_code == 200, f'Supplier list failed: {r.status_code}'
# Test add form
r = client.get('/suppliers/add')
assert r.status_code == 200, f'Add form failed: {r.status_code}'
# Test add supplier
r = client.post('/suppliers/add', data={'name': 'Test', 'feed_url': 'http://example.com/feed.xml', 'discount_percent': '15'}, follow_redirects=True)
assert r.status_code == 200, f'Add POST failed: {r.status_code}'
assert b'Test' in r.data, 'Supplier not in list after add'
print('ALL SUPPLIER CRUD TESTS PASSED')
"</automated>
  </verify>
  <done>Supplier list page renders at /suppliers/ with table. Add form at /suppliers/add creates a new supplier. Edit form at /suppliers/ID/edit updates supplier. Toggle at /suppliers/ID/toggle flips enabled status. All pages use Bootstrap 5 layout with navbar.</done>
</task>

</tasks>

<verification>
1. `uv run python run.py` starts Flask dev server on localhost:5000 without errors
2. Browser at http://localhost:5000 shows index page with navigation
3. http://localhost:5000/suppliers/ shows empty supplier list with "Add Supplier" button
4. Adding a supplier with name "MARESTO", URL "https://mrst.com.ua/include/price.xml", discount 15% succeeds
5. Editing the supplier changes values in the database
6. Toggling enable/disable changes the supplier status badge
7. `instance/labresta.db` exists and contains suppliers, prom_products, supplier_products tables
</verification>

<success_criteria>
- Flask app runs on localhost:5000
- All three database models are created (suppliers, prom_products, supplier_products tables)
- Supplier CRUD fully functional: add, edit, toggle enable/disable
- Bootstrap 5 UI with navbar and responsive layout
- No hardcoded secrets in committed files
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-risk-validation/01-01-SUMMARY.md`
</output>
