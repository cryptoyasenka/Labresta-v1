# QUESTIONS — что нужно от Yana

Обновлено 2026-05-18 под **финальную схему** (узкий фид без имени). Синхронизировано с
PLAN.md / UI-MOCKUP.md. GATE пройден; Route, фото-политика, способ доставки выбраны.

---

## ✅ РЕШЕНО (2026-05-18)

| Был / тема | Решение |
|---|---|
| Route A или B | **Route A**, рекуррентно (тело/фото/цена меняются — не разовый импорт) |
| Способ доставки | **Отдельный узкий YML-фид** (образец `sync_prices`/`sync_availability`, собственный offer-цикл), стабильный URL. НЕ общий per-supplier фид |
| Состав offer | `id`+`vendorCode`+`price`+`currencyId`+`oldprice`+`available`+`<description>`(UA)+`<description_ru>`+`<picture>`×N |
| Цена/наличие в этом фиде | **Да** — «так само як в основному фіді вигрузки після матчу» (решение Yana) |
| Имя `name`/`name_ru` | **Исключено из фида** ✅ подтв. Yana 2026-05-18. **ЭМПИРИЧЕСКИ доказано 2026-05-19:** на превью импорта Horoshop профиль крест-маппит `<name>`(укр «Хліборізка») → русское «Название модификации(RU)» — имя в фиде = регрессия RU-названия + коллизия с W1/W2. No-name дизайн верен. На канарейке имя → «Не імпортувати» |
| Фото | **Вся галерея**: `pp.image_url`=1-е фото, `pp.images` JSON=остальные `;`-URL |
| Описание | **UA+RU обязательно**, оба грузятся в Horoshop |
| Q-перезапись (затирать ли существующее тело) | **Перезаписывать UA+RU из фида безусловно** (Yana 2026-05-18: «на хорошопе много ошибок, тело часто не соответствует товару — потому и берём описание у поставщика»). Вариант «сохранять прозу» ОТКЛОНЁН (законсервировал бы брак). См. FINDINGS Рев.5 |
| Whitelist | Переиспользовать `custom_feed` (match_ids→token→стабильный URL); охват = scope 9 брендов и/или ручной выбор на `/matches` |
| Объём 1-го прогона | Сначала **только матченные**; немтаченные ≈320 — отдельный хвост (Фаза 7) |
| Тест | **Сначала 1 товар** (канарейка HKN-PICO12M), потом масса |
| Исчезновение из фида | Существующий механизм **«Зниклі матчі»** (sync_pipeline `last_seen_at`/`needs_review`). Новой политики не надо — **Q-дрейф ЗАКРЫТ**; тело pp НЕ зануляем |
| Тайминг кода vs W1 | Спек+мокап сейчас (вариант a); код в репо — после сигнала «main свободен» |
| Q7 (пустые описания) | По всему фиду 0 пустых (uk и ru у всех 690). Дубль ru==uk — редкий, см. Q-перезапись |

**Профиль импорта Horoshop больше НЕ топ-блокер:** имени в файле нет → перетереть имя
нечем, даже если профиль «обновляй всё». Остаётся обычная проверка на тесте-1 (Фаза 4),
что профиль вообще принимает `description`/`description_ru`/`picture` из этого URL.

---

## 🔴 Критичные (Фаза 0 / Фаза 1)

### Q4 — Авторитетный read-only прод-снимок матчинга
Локально 370/690 (~54%). Нужен свежий прод-снимок (Фаза 1, `snapshot_np_match.py`,
**только чтение**, к проду rlwy.net/railway НЕ подключаюсь — снимаешь ты сама или даёшь
read-only окно). Дополнительно: сколько матчей `published=1` (в фид попадают только они)
и разбивка по 9 брендам.

### 0.5 — Сигнал «W1 закончил main»
До явной отмашки Yana код Фазы 3 в репо НЕ пишется (W1 работает на main; любая
запись/коммит/checkout его ломает). Это человеческий блокер — не угадывается.

---

## 🟠 Важные (влияют на код Фазы 3)

### 0.3 — Где хранить `description_ru` на стороне sp?
`SupplierProduct.description` — одно поле, без ua/ru. Варианты:
(a) новая колонка `SupplierProduct.description_ru` (аддитивная мини-миграция,
`scripts/migrate_add_sp_description_ru.py`) — **рекомендация**;
(b) пара в `params` JSON; (c) не хранить на sp — write-through сразу в `pp.description_ua/ru`.

### ~~Q-перезапись~~ — ✅ ЗАКРЫТО (Yana 2026-05-18, см. таблицу РЕШЕНО + FINDINGS Рев.5)
Решение: перезаписывать UA+RU из фида безусловно — живые тела Horoshop ненадёжны
(часто не соответствуют товару), фид поставщика = источник правды. Это и есть
смысл задачи. Бэкап карточки в админке Horoshop (CANARY-IMPORT-GUIDE §4 шаг 0)
остаётся как откат, но «сохранять существующую прозу» отклонено.

### Q6 — Порядок относительно W1/W2
Поля непересекающиеся (W1/W2=имена; эта задача=цена+наличие+описание+фото; имени в фиде
нет). Рекомендую массовый прогон (Фаза 5) **после** прохода W1/W2 по затрагиваемым chunk —
одно живое изменение за раз (инвариант #13). Тест-1 (Фаза 4) можно раньше. Согласна?

---

## 🔵 НОВАЯ ЗАДАЧА — W3: перевод имён/тайтлов НП SKIP-товаров (Yana 2026-05-19)

W1/W2 помечают 9 НП-брендов SKIP-НП → их `name`/`name_ru`/title сейчас **никто
не переводит** (тело придёт из НП-фида, имя — дыра, подтверждено превью-крестмапом).
Предложение Yana: я = **W3**, делаю только НП-бренды (`supplier_id=2`, matched +
`published=1`) — множество непересекающееся с W1/W2, поля те же, товары другие →
коллизий по данным нет.

**Статус:** идея принята, НЕ начата. Оформить отдельно (`/gsd:discuss-phase`
или короткий план в plan-каталоге) ПОСЛЕ результата канарейки.

**Открытые вопросы скоупа (снять до старта):**
- W3-Q1: НП-источник даёт переводимое имя для этих карточек, или `pp.name_ru`
  уже есть, но кривой (как было с `description_ru`)? — read-only снимок, не гадать.
- W3-Q2: куда W3 пишет, чтобы не столкнуться с W1 на `main` (репо read-only) —
  за сигналом 0.5 либо свой worktree. Решаем при скоупе.
- W3-Q3: критерий «готово» + формат как у W1/W2 (chunks в `.planning/translation-audit/`).
- W3-Q4: связь с НП-фидом тело+фото — один проход на товар или раздельно
  (инвариант #13: одно живое изменение за раз).

## 🟡 Мелкие / на потом

### 0.2 — Whitelist по умолчанию
Вся 9-брендовая выборка одной кнопкой / ручной выбор на `/matches` / оба режима.
Мокап заложил **оба**. Подтвердить дефолт.

### 0.4 — Точки UI-MOCKUP.md
Где пункт меню (новый раздел «Фіди ▾» или внутри `/feeds/custom`); нужно ли превью
состава до генерации (мокап показывает — полезно); формат лога пропущенных
(нет фото / нет ru / 404 фото).

### Q9 — Узкий пробел U+202F в числах тела
Нормализовать в обычный пробел при импорте или оставить (обычно ок). Дефолт — оставить,
проверить на тесте-1.

### Q10 — Бренды с 0 карточек: Project Systems / Astoria / Maxima
В фиде есть (4/3/1), в локальном каталоге Horoshop карточек 0. Они на сайте под другим
написанием бренда, или их нет (тогда тело лить некуда — вне scope до доматчивания, Фаза 7)?

### Q11 — Ретенция бэкапа
Дефолт: бэкап «до» хранить до подтверждённого стабильного рекуррентного цикла + 30 дней. Ок?

---

## Топ-блокеры, без которых не стартуем
1. **0.5 Сигнал «W1 закончил main»** — до него код Фазы 3 не пишется (человеческий блокер).
2. **Q4** — авторитетный read-only прод-снимок (объём = матченные + `published=1`).
3. **0.3** — место хранения `description_ru` (влияет на код Фазы 3, не блокирует дизайн).
   (0.1 имя — ✅ ЗАКРЫТО: исключено, см. РЕШЕНО.)
