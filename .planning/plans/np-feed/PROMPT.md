# Промпт для окна планирования НП-фид (описание + фото)

Разовое ПЛАНИРОВАНИЕ. НЕ /loop. Вставить в свежее окно Claude (cd C:\Projects\labresta-sync), один раз Enter.

---

PLANNING-ONLY. Не исполнять план, не коммитить, не трогать БД/Horoshop. Ты — планировщик, не воркер.

ЦЕЛЬ: составить план обновления ОПИСАНИЯ (тело товара) и ФОТО для товаров 9 НП-эксклюзивных брендов из фида поставщика «Новый проект» (НП) в LIVE Horoshop-магазине LabResta. Это отдельная задача, параллельная аудиту перевода (W1/W2 эти товары намеренно помечают SKIP-НП — их тело придёт именно из этого фида).

БРЕНДЫ (scope строго эти, сравнение без регистра, латиница И кириллица): HURAKAN/Хуракан, APACH/Апач, FAGOR/Фагор, TATRA/Татра, COLD/Колд, PROJECT SYSTEMS, ASTORIA, ARRIS, MAXIMA.

РЕЖИМ БЕЗОПАСНОСТИ (жёстко, не нарушать):
- cd C:\Projects\labresta-sync — ТОЛЬКО для ЧТЕНИЯ кода/схемы/доков (Read/Grep/Glob; read-only git: log/show/status).
- НИЧЕГО не писать и не коммитить внутри C:\Projects\labresta-sync — там одновременно работает воркер W1 на main, любая запись/коммит/checkout сломает его. git add/commit/push/checkout/worktree — ЗАПРЕЩЕНО.
- Все артефакты (черновики, скрипты-пробы, дампы, план, вопросы) писать ТОЛЬКО в C:\Users\Yana\labresta-np-feed-plan\.
- НИКАКИХ записей в БД, НИКАКОГО импорта в Horoshop, НИКАКОГО изменения товаров. Только анализ и план.
- БД: к проду (rlwy.net / railway.app) не подключаться вовсе. Gate-проверку строй на чтении самого фида, не БД.

GATE — главный вопрос, без него план бессмысленен:
1. Найди в коде/конфиге, где определён фид поставщика «Новый проект» (URL/путь, формат — XML/YML/Prom/Horoshop). Если URL есть — скачай (только GET) и распарси.
2. На МНОГИХ строках (выборка ≥30 случайных товаров, НЕ 1) проверь: поле описания RU реально ОТЛИЧАЕТСЯ от описания UA (в фиде настоящий русский текст, а не дубль украинского). Выведи: сколько строк проверено, на скольких ru!=uk, 3-5 примеров пар. Это решает, можно ли вообще лить тело из НП.

ПЛАН (файл PLAN.md, по фазам) должен покрыть:
- Структура НП-фида: поля описания ru/uk, фото-URL'ы, артикул/идентификатор сопоставления.
- Сопоставление НП-товар ↔ карточка LabResta по артикулу, с правилом article-anchor (sp.article == pp.display_article И artikul встречается в pp.name/name_ru — свериться с кодом аудита и доками проекта). Сколько SKIP-НП SKU реально матчится, сколько нет.
- Что обновляем: ТОЛЬКО тело (описание) + фото. НЕ цена, НЕ название, НЕ поля перевода (их правят W1/W2). Явный whitelist полей. Подтвердить что частичный апдейт Horoshop по артикулу не затирает прочие поля.
- Фото: как Horoshop принимает фото при импорте (URL в фиде? замена/добавление?), порядок, главное фото.
- Бэкап: скрипт бэкапа затрагиваемых карточек + dry-run восстановления ДО любой записи (обязательно).
- Безопасный прогон: dry-run → выборка N товаров → ручная проверка Yana → только потом полный, и только с явным go-ahead + safe-mode. Никогда не полный импорт без отмашки.
- Порядок vs аудит перевода: импорт тел запускать ПОСЛЕ завершения W1/W2 (или обосновать иное). Зафиксировать что коллизии с переводом нет (SKIP-НП SKU аудит намеренно не трогает).
- Риски и откат.

ДЕЛИВЕРЫ — только в C:\Users\Yana\labresta-np-feed-plan\:
- PLAN.md — фазовый план с конкретными шагами и спецификацией скриптов (спецификация, не запуск).
- QUESTIONS.md — что нужно от Yana (URL/доступ к НП-фиду, подтверждение полей, окно для live-операции) + открытые решения.
- FINDINGS.md — результат GATE (ru!=uk: статистика + примеры) + статистика матчинга.

STOP: когда PLAN.md + QUESTIONS.md + FINDINGS.md написаны — напечатай краткое резюме (можно ли лить тело по итогам GATE, % матчинга, топ-3 вопроса к Yana) и ЗАВЕРШИ. План не исполнять. /loop НЕ запускать — это разовое планирование, не петля.
