# =============================================================================
# graphify ignore — защита от утечки секретов и клиентских данных в Claude API
# =============================================================================
# Любой файл, не исключённый здесь, будет прочитан graphify и его содержимое
# (или производное) уйдёт в Anthropic API при семантической экстракции.
# Правило простое: если сомневаешься — добавь в исключения.

# --- Секреты и креды ---------------------------------------------------------
.env
.env.*
*.env
secrets/
credentials*

# --- БД и её бэкапы ----------------------------------------------------------
instance/
backups/
*.db
*.db-shm
*.db-wal
*.db.bak*
*.sqlite
*.sqlite-*

# --- Прайсы поставщиков и выгрузки клиента (всё что Yana заливает руками) ----
*.xls
*.xlsx
*.XLSX
*.pdf
horoshop-export*
tmp_*

# --- JSON/CSV/MD дампы аудитов и отчётов -------------------------------------
audit_*.json
audit_*.md
audit_*.txt
red_*.json
red_*.md
red_*.txt
np_*.json
np_*.md
np_*.txt
labresta_x_*.json
labresta_x_*.md
reports/
RED_TODO.md
LABRESTA_X_REPORT.md

# --- Стратегические рабочие черновики Yana ----------------------------------
# Содержат имена поставщиков, конкретику цен, домен-специфику
.planning/

# --- Локальная конфига Claude Code и аккаунт-свитчеры ------------------------
.claude/
claude-acc*.bat

# --- Стандартный мусор -------------------------------------------------------
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.venv/
node_modules/
dist/
*.egg-info/
.git/

# --- Логи --------------------------------------------------------------------
*.log
logs/

# --- Сам graphify-out --------------------------------------------------------
graphify-out/

# --- CLAUDE.md проекта -------------------------------------------------------
# Может содержать домен-конкретику и инструкции с реальными именами/цифрами
CLAUDE.md
