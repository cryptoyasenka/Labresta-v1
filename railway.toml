[build]
builder = "nixpacks"

[deploy]
startCommand = "python scripts/migrate_add_feed_name_pg.py && gunicorn run:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
